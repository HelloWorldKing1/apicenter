# 平台入站鉴权设计方案（调用方鉴权 · 回调验签 · 接入审计）

> **版本**：**v1.1**（设计稿，**待评审**，2026-09-23 复核优化；初版 2026-09-19）
>
> **v1.0 → v1.1 变更**（本次按**实际代码**逐条复核后修正/补充，全部围绕"闸门会不会把自家功能打死"）：
> ① **新增 D-CA-17 + §3.4**：**平台自身发起的调用必须豁免闸门** —— 管理面「模拟回调」`/test-callback`
>    是**自调网关**（`InterfaceController.testCallback` → `POST http://localhost:{port}{platformPath}`），
>    `ENFORCED` 下会被闸门拒 ⇒ **该调试工具全废**（而验收方案正依赖它）；另含 `OPTIONS` 预检、前置编排、worker 三类内部路径的逐一交代。
> ② **修正 §10.1 的事实错误**：原文写"`/test`、`/test-callback` 都不经网关"——**错**，只有 `/test` 不经；
>    `/test-callback` 自调网关，**会**过闸门（走 ① 的豁免并写审计）。
> ③ **修正 §0 对 `call_log` 字段的列举**（原列举漏了 `interface_id/app_id/trace_id` 等，结论不变）。
> ④ **§6.1 增加"步骤 ⓪"**（`OPTIONS` 与内部令牌先判）；**§12 补** 40103 双维度的文案区分；
>    **§14 说明内部令牌无需配置项**（启动随机生成、进程内比对）。
> ⑤ **§16/§17/§18/§20 同步**：新增 4 例测试、2 处改动面、1 条待评审项、2 条坑预判。
> **定位**：把「**谁在调平台**（调用方）/ **谁在回调平台**（供应商）」这件事补上——入站方向一次
> **可配置、可审计、可灰度**的鉴权验证过程，并留痕「**调用方 IP · 名称 · 鉴权方式 · 结果**」。
>
> **覆盖**：平台对外接口路径 `/{platformPath}`（Flow A 出站中转调用 / Flow B 供应商回调）。
> **不覆盖**：管理面 `/api/admin/**`（已有 `AdminAuthFilter` + `admin_user/admin_session`，见《账号登录设计方案.md》）、
> 平台作为调用方向供应商签名（已有 `AUTH` 角色 / `app_credential`）、向回调地址签名（设计 §5.3「可选，默认无」）。
>
> **落地状态**：**B1「数据与目录」+ B2「鉴权内核」已落地（2026-09-23）**；B3–B6 待做。
> **B1 落地摘要**：3 表 DDL（`client_app`/`client_credential`/`access_auth_log`，第 23/24/25 张，开发库已建）
> + `CredentialOwner` 枚举 + `CredentialRepository` 按属主列参数化（应用侧入口保留为委托，零回归）
> + **抽出 `CredentialStore`**（机制）+ `CredentialService`（应用）/ `ClientCredentialService`（调用方）两个语义入口
> + `ClientAppRepository`/`ClientService` + `/api/admin/clients/**`（CRUD / 启停用 / 凭证子资源）
> + 删适配器时同步清 `client_app.auth_adapter_id`（§4.2）。
> 测试：`ClientAuthIntegrationTest` 9 例全绿；回归 `M1IntegrationTest`/`HmacCallbackVerifyAdapterTest` 全绿（表数断言同步 22→25）。
>
> **B2 落地摘要**：`InboundAuthAdapter` 契约（`method()`/`credentialKind()`，**适配器不查库**，凭证明文经
> `ctx.attrs("inboundCredentials")` 注入）+ 4 个 impl（`ClientApiKeyVerifyAdapter`/`ClientHmacVerifyAdapter`/
> `ClientBearerVerifyAdapter`/`ClientIpWhitelistVerifyAdapter`，已登记进 `AdapterImplCatalog` 供前端自动渲染）
> + `ClientAuthVerifier`（§6.1 五步 + **步骤 ⓪**：`OPTIONS` 豁免与 `X-Internal-Token` → `PLATFORM_SELF`）
> + `ClientAuthProperties`（`mode` 三态）+ `InternalCallToken`（启动随机生成、仅存内存、常量时间比较、**空令牌不豁免**）
> + `AccessAuthContext`（审计上下文，B3 落库）+ `SensitiveDataMasker` **动态注册头名**（D-CA-13 硬要求）。
> 测试：`ClientAuthVerifierTest` 23 例（**§6.2 真值表逐行** + 反证「不回退 Noop」+ 内部令牌 + IP 维度 + 审计字段）
> + `InboundAuthAdaptersTest` 10 例（含 **HMAC 与回调验签同口径**：同一签名两边都通过）+ `SensitiveDataMaskerTest` +2 例（动态注册）＝ 33+2 全绿。
> 第 19 节给出批次与人日；第 20 节给出坑预判。

---

## 0. 先思考：现状盘点与结论（设计起点）

现状是三套机制各自为政，缺口清晰：

| # | 机制 | 主体 | 执行位置 | 现状 |
|---|---|---|---|---|
| ① | `AdminAuthFilter` + `admin_session` | 平台运营人员 | Servlet Filter（`/api/admin/**`） | ✅ 已落地（2026-09-18） |
| ② | **调用方鉴权** | 平台客户（ERP / 业务系统） | —— | ❌ **完全没有**——`GatewayController` 路由命中即执行，任何知道路径的人都能调 |
| ③ | 回调验签 `CALLBACK_AUTH` | 供应商 | 链内 `INBOUND_AUTH`（`HmacCallbackVerifyAdapter`） | ⚠️ 有，但**只有 HMAC 一种方式**，且**无 IP / 方式留痕** |
| ④ | `GatewayGuard` | 应用（供应商） | 网关入口 | ⚠️ 有 QPS / 日配额 / IP 名单，但**无「调用方」维度** |

**四件事目前都做不到**（逐条对应本设计要解决的问题）：

1. **调用方无法鉴权**——`ChainEngine.assemble` 里 Flow A 的 `INBOUND_AUTH` 阶段是**硬编码 Noop 占位**：
   ```java
   // 1. 入站鉴权：Flow A 调用方鉴权属平台统一能力（范围外）→ Noop 占位
   ```
   设计 §3.1 / §5.3 明确写着「调用方鉴权由平台统一处理，**不在本模型内**」——《API中心项目说明.md》§2
   甚至把它列为「平台**不做**的事」。**本设计就是把这句「范围外」落地成「范围内」。**

2. **没有「调用方」这个主体**——`app` 表 = 供应商（既是归属也是转发目标），全库**没有任何表**描述
   「谁在调用平台」。所以「记录调用方的名称」无处可取。

3. **没有记录来源 IP 的地方**——`call_log` 有 `direction/url/method/status_code/interface_id/app_id/step_code/latency_ms/trace_id/req_*/resp_*`，但**没有 `client_ip` 列**，也**没有鉴权主体 / 方式 / 结果**；
   `outbound_request` / `inbound_delivery` 更没有。事后想回答
   「刚才那批 401 是谁打过来的」——**查不到**。

4. **没有「鉴权方式」的口径**——`HmacCallbackVerifyAdapter` 验签失败只抛 `BizException(40100)` +
   `alert_event`（`metric=verify_fail_streak`），**不记录用了哪种方式、哪个适配器实例、主体是谁**。

**由此得到本设计的三个判断**：

- **判断 1（主体模型）**：调用方必须是**独立于 `app` 的新主体**。`app` 是「被代理的一方」，
  调用方是「发起调用的一方」，把调用方塞进 `app` 会让 `app_id` 这个对外契约（URL / 凭证归属 /
  监控过滤）语义崩塌。→ 新增 `client_app`，与 `app` **对称设计**（目录表 + `adapter`(auth)
  引用 + 独立凭证表 + 名单/限流字段）。
- **判断 2（执行位置）**：调用方鉴权归**网关入口闸门**（`GatewayController` 内、引擎之前），不归链内
  ——理由是设计原话「平台统一处理，不在接口模型内」：它**不进接口配置快照、不受链缓存影响、
  拒绝不落 `outbound_request`**（与 M4 「防护拒绝不污染状态机」同一条纪律）。
  而回调验签**留在链内**（M3 已验证的 `CALLBACK_AUTH` + 绑定解析 + 链缓存），**不动**——
  见 §3.3 的取舍理由。
- **判断 3（统一的是口径，不是位置）**：两处执行、**四样东西必须统一**——**鉴权方式目录**（同批适配器
  impl）、**凭证注入契约**（适配器不查库）、**审计表**（`access_auth_log` 一张）、**错误码**
  （401xx 段）。这样「调用方鉴权」与「回调验签」在运维视角是**一个功能**，在实现视角是**两处薄胶水**。

### 0.1 顺带发现的两个既有缺陷（本设计一并修正）

| # | 缺陷 | 事实 | 影响 | 处置 |
|---|---|---|---|---|
| B1 | **可配置的密钥头名不进脱敏清单** | `SensitiveDataMasker` 的敏感头是**写死清单**（`authorization` / `x-api-key` / `x-auth-token` / `x-partner-signature` / `x-signature` …）；而 `ApiKeyAuthAdapter` / `HmacCallbackVerifyAdapter` 的**头名是适配器参数可配的**（`headerName` / `signatureHeader`） | 配成 `appKey` / `X-Custom-Sign` 时，**密钥/签名明文落 `call_log.req_headers`** | 敏感头名**动态注册**（§6.3 D-CA-13）：鉴权适配器声明的头名 + 配置项 `client-auth.mask-header-names` 一并进 masker |
| B2 | **应用级 IP 名单对 OUTBOUND 语义错位** | `GatewayController` 用 `routed.appId()`（**供应商**）的 `ip_whitelist` 去校验**调用方**的来源 IP | 某供应商应用为「回调来源保护」配了白名单，会**连带拦掉该供应商出站接口的正常调用方** | **本次不改行为**（兼容 M4 `c8` 用例），但**在文档中把语义钉死**并给待拍板项（§12.2）：是否另立 `callback_ip_whitelist` |

> B2 的处理体现一条纪律：**发现语义歧义时，先记录+给选项，不在鉴权设计里顺手改别人的行为**。
> 详见 §12.2 待拍板 D-CA-11。

---

## 1. 目标与非目标

| | 内容 |
|---|---|
| **目标** | ① 调用方（平台客户）调平台时**强制可配的鉴权**（API Key / HMAC / Bearer / 仅 IP 名单）② 供应商回调验签**方式扩展**（从只有 HMAC 扩到同一批方式）③ **接入审计**：每次鉴权判定留痕「主体（id + 名称）· 来源 IP · 鉴权方式 · 结果 · 失败原因 · traceId」④ 调用方维度的 IP 名单（40103 复用）⑤ 管理面「调用方管理」页 + 「接入鉴权」审计查询⑥ **可灰度**（观察 → 可选 → 强制三态），不搞一刀切上线的惊喜 ⑦ 失败告警（连续失败，复用 `AlertService` 口径） |
| **非目标** | ① **授权**（调用方能访问哪些接口的 ACL —— 鉴权 ≠ 授权，P2 见 §18）② mTLS / OAuth2 client_credentials / Basic（P2/P3）③ 分布式凭证/防重放（单实例内存口径，与熔断/限流一致，v1.1）④ 临时封禁（v1.1，与 M4 结论保持一致）⑤ 管理面 SSO / 操作审计表 ⑥ 向回调地址签名（设计 §5.3 明确「可选，默认无」，本次不启用） |

---

## 2. 决策记录（D-CA-*，评审重点）

| # | 决策 | 理由 / 备选（含被否方案） |
|---|---|---|
| **D-CA-1** | **调用方 = 新主体 `client_app`**，不复用 `app`，不塞进 `app_credential` | `app` = 供应商（被代理方），语义互斥；复用会让 `app_id` 契约崩塌（URL/凭证归属/监控过滤全绑它）。备选「`app_credential` 加 `owner_type/owner_id` 多态」被否：改的是 M0-04 已验证的核心表 + 其函数唯一索引议题，风险远大于新表 |
| **D-CA-2** | **调用方鉴权在网关入口闸门执行**（`GatewayController` 路由命中后、引擎之前），**不进链、不进接口模型** | 设计原话「由平台统一处理，不在接口模型内」；好处：不污染 `outbound_request`（拒绝发生在 `createRecord` 前，与 D-M4-6 同口径）、不进接口快照/版本 diff、不受链缓存 TTL 影响 |
| **D-CA-3** | **入站鉴权按 `if_type` 二分**：`OUTBOUND` → 调用方鉴权（闸门）；`INBOUND` → 回调验签（链内 `INBOUND_AUTH`，M3 既有，**不搬迁**） | 一个接口的入站方**只可能是其中之一**（出站中转的入站方=调用方；入站回调的入站方=供应商），与设计 §3.1「类型互斥」一致。**不把回调验签搬进闸门**的理由：绑定解析（接口覆盖→应用默认→平台默认）+ 链缓存烘焙 + `rawBody/headers` 通道全在 `ChainEngine`，搬迁等于重写 M3 已验证路径（见 §3.3） |
| **D-CA-4** | **显式主体标识头** `X-Client-Id`（头名可按调用方配置），凭证走各自 Header / 签名串 | 与既有「`appId` + secret」心智模型对称；O(1) 定位调用方（无需密钥摘要反查、无需给凭证表加 UNIQUE 哈希列）；审计的「名称」直接可得。备选「单头 + 密钥 SHA-256 反查」被否：HMAC/Bearer 无法由摘要定位（HMAC 要 secret 才能验），且多一套索引 |
| **D-CA-5** | **鉴权方式复用 `adapter`（`type=auth`）配置化**：新增 4 个 `INBOUND_AUTH` 验签 impl，调用方经 `client_app.auth_adapter_id` 引用（无应用/接口级继承） | 复用 D6' 适配器模型（`adapter.name` 全表唯一 / 绑定即实例 / 停用即回退）+ `AdapterImplCatalog` 元数据 → 前端表单**自动渲染**（`AdapterParamsEditor`），零新增方式专表；「鉴权方式」在审计里 = impl 语义名 |
| **D-CA-6** | **契约扩展 `InboundAuthAdapter`**（`Adapter` 的子接口）：`method()` 审计口径 + `credentialKind()` 期望凭证类型；**适配器不查库**，凭证由注入方经 `ctx.attrs("inboundCredentials")`（`List<String>`，ACTIVE+ROTATING 并存）提供 | 修正既有 `HmacCallbackVerifyAdapter` **直接查库**的耦合（适配器本该无状态、配置驱动，M0-01 §5.2）；同一适配器可同时服务于「调用方鉴权（凭证来源 `client_credential`）」与「回调验签（凭证来源 `app_credential(kind=CALLBACK)`）」——**唯一差异就是注入方**，正好呼应 M0-04「两类凭证分离」。存量 impl 保留兼容（§9.3） |
| **D-CA-7** | **默认 fail-closed**：启用后，调用方**未配鉴权方式 / 无可用凭证 → 拒绝**（40108 / 40100） | 「没配 = 放行」是最常见的鉴权事故。唯一例外是显式的 `IP_WHITELIST` 方式（且白名单必须非空，否则拒） |
| **D-CA-8** | **三态灰度开关** `client-auth.mode = OFF \| OPTIONAL \| ENFORCED`（默认 `OFF`），**平台级唯一**；接口级豁免走调用方 ACL（P2） | 一次性强制 = 线上全量 401。三态 + 观察期（§16）是安全上线的前提。**不新增 `interface` 列**（否则动 DTO / 快照 `config_json` / 版本 diff / 前端表单，改动面翻倍） |
| **D-CA-9** | **审计独立表 `access_auth_log`**，**PASS / REJECT 全记**（可配只记失败），**异步批量写**（复用 `CallLogWriter` 模式），默认保留 90 天 | 不塞 `call_log` 的理由：① 审计要按「主体 / IP / 结果」检索、要独立保留期，与按 trace 检索的高频日志是两种用途；② `call_log` 已「列表瘦身」且**加列要同步三处**（`insertBatch` 位置绑定序号 / `LIST_COLUMNS` / `CallLogView`+`toView`），错位会**静默写错列**；③ 拒绝请求可能发生在 `CallLogContext` 填充**之前**（闸门比引擎早），`call_log` 那行 `interface_id/app_id` 恒空，承载不了主体信息 |
| **D-CA-10** | **IP 名单归「鉴权」，限流/配额归「防护」**：调用方维度 IP 名单进闸门（40103）；QPS / 日配额仍留 `GatewayGuard`（42901/42902），位置在闸门**之后** | 职责切干净：**「你是谁」= 鉴权（401xx，一次判定）**；**「放你多少」= 防护（429xx，计数态）**。IP 拒绝也是鉴权结论，必须进审计表；限流是配额，不进审计（它已有 `Counter{outcome=rejected}` + `call_log`） |
| **D-CA-11** | **应用（供应商）维度的 IP 名单 / 配额行为本次不动**，仅**语义钉死 + 文档澄清**；是否另立 `callback_ip_whitelist` **待拍板** | 动它会破坏 M4 `c8_QPS限流_超限42901_不落运行表`（该用例用 OUTBOUND 接口 + `app.qps_limit`）。**调用方维度与应用维度是 AND 叠加**（任一拒绝即拒），这样既有配置继续有效、新维度更细。详见 §12.2 |
| **D-CA-12** | 错误码：**复用** 40100（凭证/签名不匹配）/ 40101（时间戳超容差）/ 40103（来源 IP 被拒）；**新增** 40107（调用方未知 / 已停用）、40108（调用方未配置鉴权方式）；HTTP 统一 401，`msg` **描述性** | 40100/40101/40103 已是「入站鉴权」语义，复用它让调用方与供应商的**联调说法一致**。描述性 msg 是取舍（有轻微枚举信息泄露），选「运维可调试优先」——与 FastMoss 联调时「先取证再二分」的团队习惯一致（§6.6 记录取舍） |
| **D-CA-13** | 安全口径：凭证 **AES-256-GCM 密文**（复用 `CryptoService`）+ 接口**只回尾 4 / 指纹**（复用 `CredentialService` 口径）+ **常量时间比较**（`MessageDigest.isEqual`）+ 敏感头名**动态注册**进 `SensitiveDataMasker` + 审计只记 `client_ip`/`xff_chain`/UA 截断 | 与 M0-04（凭证不落明文）、账号登录（令牌只存摘要）同一条纪律。B1 缺陷说明「写死脱敏清单」在「可配头名」下必然漏，所以**动态注册是硬要求** |
| **D-CA-14** | 管理面：新增**「调用方管理」页**（`views/Clients.vue` + 路由 + 菜单）+ 监控页新增**「接入鉴权」Tab**（审计查询） | 与既有页面结构对齐（Apps / Users / Monitor 五区块）。写端点自动受 `AdminAuthFilter` 的 VIEWER 只读规则保护（40302），**无需新增角色代码**（§13.3） |
| **D-CA-17** | **平台自身发起的调用必须豁免闸门**（新增，v1.1）：管理面「模拟回调」`/test-callback` **自调网关**（`POST http://localhost:{port}{platformPath}`）⇒ `ENFORCED` 下会被闸门拒，**该调试工具直接失效**（验收方案 §3.1 正依赖它）。方案：**内部调用令牌** —— 启动时 `SecureRandom` 生成（仅存内存，**不进配置/不落库**，多实例各自独立即可，因为自调是**本实例到自己**），自调时带 `X-Internal-Token`；闸门**先判令牌**（常量时间比较），命中即 `PLATFORM_SELF` 放行并**仍写审计**。**禁止**用"来源是回环 IP"作豁免判据（反代/容器下可被伪造） | 见 §3.4 |

---

## 3. 总体架构

### 3.1 三类主体（一次说清，避免术语漂移）

| 主体 | 表 | 谁 | 平台与它的关系 | 鉴权机制 |
|---|---|---|---|---|
| **运营人员** | `admin_user` | 平台自己的运维/管理员 | 内部 | `AdminAuthFilter`（`/api/admin/**`） |
| **调用方 / 接入方**（新） | **`client_app`** | 平台客户（ERP、业务系统） | 平台**签发**凭证给它 | **调用方鉴权**（本设计，网关闸门） |
| **供应商 / 应用** | `app` | 被代理的第三方（FastMoss、evoLink…） | 平台**持有**它的凭证 | **回调验签**（M3，链内）+ 出站签名（`AUTH`） |

> **术语纪律**（沿用 2026-09-08 定稿）：对用户可见文案继续用「供应商」指被代理方；本设计新增的
> **「调用方」= 平台客户**，文档与 UI 统一用「调用方」（不叫「客户端 / 商户 / 租户」）。
> 唯一例外是**线协议头名 `X-Client-Id`** 与**表名 `client_app`**（HTTP 语义与既有 `app_id` 对称，避免 `X-Caller-Id` 与
> `X-Biz-Id` 混淆）。

### 3.2 执行顺序（网关入口，改造后全景）

```
GatewayController.gateway(path, body, request)
 1. 路径归一化 / traceId（TraceIdFilter 已写入属性）/ bizId / 报文大小兜底（40002）
 2. 路由解析：interfaceRepository.findByPath(fullPath)  →  iface（供应商应用 = app）
 3. 【新】入站鉴权闸门 GatewayInboundAuth.authenticate(request, iface, body)
      ├─ if_type = OUTBOUND → 调用方鉴权 ClientAuthVerifier
      │    ① 主体识别（X-Client-Id → client_app；缺失/未知/停用 → 40107）
      │    ② 调用方 IP 名单（黑名单优先 → 白名单非空未命中 → 40103）
      │    ③ 鉴权方式解析（client_app.auth_adapter_id → InboundAuthAdapter；未配 → 40108）
      │    ④ 凭证注入（client_credential，kind = adapter.credentialKind()，ACTIVE+ROTATING）
      │    ⑤ 适配器校验（40100 / 40101 / 防重放）
      │    ⇒ 结论写入 AccessAuthContext（PASS / REJECT + method + principal + ip）
      └─ if_type = INBOUND  → 仅登记审计上下文（principal=SUPPLIER/app，method 待链内回填）
 4. GatewayGuard.check(app, clientIp)   ← 限流 / 日配额（应用维度，既有 42901/42902；P2 叠加调用方维度）
 5. 分流：INBOUND → InboundEngine.handle(...)   OUTBOUND → OutboundEngine.dispatch(...)
      └─ InboundEngine：链执行（INBOUND_AUTH 回调验签）后回填 AccessAuthContext（PASS / 401xx REJECT）
 6. CallLogAspect.aroundGateway  try/catch/finally
      ├─ 既存：IN 方向 call_log + Counter/Timer + CallLogContext.clear()
      └─ 【新】AccessAuthContext.flush() → AccessAuthWriter.offer(...)（异步批量）
```

**三处关键性质**（都要有测试钉住）：

- **不落运行表**：闸门在 `OutboundEngine.dispatch → createRecord` **之前**，鉴权失败 ⇒ `outbound_request`
  零增量（与 D-M4-6 同口径，可复用「计数不变」断言）。
- **不污染状态机**：失败只抛 `BizException`，统一信封返回（`GlobalExceptionHandler`，HTTP = `code/100`）。
- **一定留痕**：即使请求在闸门就被拒，`CallLogAspect.aroundGateway` 的 `catch` 分支照常写 IN 条 `call_log`，
  `finally` 里 flush 审计 ⇒ **拒绝也有审计行**（安全审计的核心价值）。

### 3.3 为什么不把回调验签也搬到闸门（D-CA-3 的展开）

| 维度 | 留在链内（选） | 搬到闸门（否） |
|---|---|---|
| 绑定解析 | 复用 `ChainEngine.resolveBound`（接口覆盖 → 应用默认 → 平台默认 + `enabled` 校验 + 逐层回退 + `log.warn`） | 要在闸门里**重写一套**（或把 `ChainEngine` 的方法暴露成公共 API，耦合反向） |
| 链缓存烘焙 | 已在装配期烘焙（M5 D-M5-2），零额外成本 | 每请求实时解析（或另建一套缓存 + 事件失效） |
| 请求上下文 | `ctx.attrs("headers"/"rawBody")` 现成 | 要为闸门另造 `AdapterContext` 构造路径（本设计为调用方鉴权造了，但没必要再造一份给回调） |
| M3 回归面 | **零**（`HmacCallbackVerifyAdapter` 不动，只在 `InboundEngine` 加一处 try/catch 回填审计） | 全量重测 M3 回调验签（B1-B6 + X1-X3） |
| 代价 | 两处执行点 | 一处执行点，但绑定解析双份实现 |

**结论**：统一「口径」（方式目录 / 凭证契约 / 审计 / 错误码），**不统一「执行点」**。
`InboundEngine` 里只加一处 5 行的 try/catch 回填审计（§9.2）——这是本设计对 M3 的唯一改动。

---

### 3.4 平台自身发起的调用（内部路径，闸门必须区分）

> 闸门在 `GatewayController` 内 ⇒ **凡走到网关的请求都会过闸门**，**包括平台自己发出的那些**。
> 逐一交代，避免实现时"漏一个就把自家功能打死"：

| 内部路径 | 经网关？ | 闸门下会怎样 | 处理 |
|---|---|---|---|
| 管理面**「模拟回调」** `POST /interfaces/{id}/test-callback` | ✅ **经**（自调 `http://localhost:{port}{platformPath}`） | **会被拒**（无 `X-Client-Id`）⇒ **工具失效** | **D-CA-17 内部令牌豁免** → `PLATFORM_SELF`，**仍写审计** |
| 管理面**「测试接口」** `POST /interfaces/{id}/test` | ❌ 不经（直调引擎） | 不受影响 | 无需处理；**也不写**审计（§10.1） |
| **前置编排**（`PreStepExecutor` → `ChainEngine`/`UpstreamInvoker`） | ❌ 不经 | 不受影响 | 无需处理（**不要**给前置调用加调用方鉴权） |
| **补偿 / 重送 worker**（出站补偿、入站重送） | ❌ 不经 | 不受影响 | 无需处理 |
| 浏览器 **CORS 预检 `OPTIONS`** | ✅ 经 | 预检**不带凭证头** ⇒ 会被拒 ⇒ 浏览器直调场景全废 | 闸门对 `OPTIONS` **豁免**（与 `AdminAuthFilter` 同口径） |
| `/actuator/**`（健康检查）、管理面 `/api/admin/**` | ❌ 不同路径 | 不受影响 | 无需处理 |

> ⚠️ **两条硬约束**（实现时最容易漏）：
> ① 内部令牌比对必须用**常量时间比较**（`MessageDigest.isEqual`），且**令牌为空一律不豁免**（禁止"空令牌放行"）；
> ② **不要**用"来源 IP 是回环地址"作为豁免判据 —— 反向代理 / 容器场景下真实客户端 IP 的取法见 §11，
>    按 IP 放行等于给外部流量开了一条后门。

## 4. 数据模型（第 23 / 24 / 25 张表）

### 4.1 DDL（完整，可直接追加到 `schema.sql`）

```sql
-- 23 调用方（平台客户 / 接入方）：与「应用（供应商）」对称的入站主体——平台签发接入凭证给它。
--    与 app 的分工：app.auth_adapter_id 管「平台作为调用方 → 供应商」的出站签名；
--    client_app.auth_adapter_id 管「调用方 → 平台」的入站鉴权（方向相反，主体相反）。
--    ⚠ 本设计落地时应用到开发库；schema 与《表结构设计.html》同步。
CREATE TABLE client_app (
    id              BIGINT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
    client_id       VARCHAR(32)  NOT NULL COMMENT '调用方标识（全局唯一，如 ERP-PROD；对外展示与审计引用）',
    name            VARCHAR(64)  NOT NULL COMMENT '调用方名称（审计留痕的主体名称来源）',
    contact         VARCHAR(64)  COMMENT '联系人',
    auth_adapter_id VARCHAR(16)  COMMENT '入站鉴权适配器（adapter.type=auth；NULL = 未配置 → 启用后 fail-closed 拒绝 40108）',
    ip_whitelist    VARCHAR(500) COMMENT '调用方来源 IP 白名单（英文逗号分隔，空 = 不限；精确匹配，CIDR v1.1）',
    ip_blacklist    VARCHAR(500) COMMENT '调用方来源 IP 黑名单（优先于白名单；命中 → 40103）',
    qps_limit       INT          COMMENT '调用方 QPS 上限（空/0 = 不限；P2 生效，见 §18）',
    daily_quota     BIGINT       COMMENT '调用方日调用量上限（空/0 = 不限；P2 生效）',
    status          VARCHAR(16)  NOT NULL DEFAULT 'ENABLED' COMMENT 'ENABLED 启用 / DISABLED 停用（停用即拒 40107）',
    `desc`          VARCHAR(500) COMMENT '描述',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_client_id (client_id),
    KEY idx_client_auth_adapter (auth_adapter_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='调用方（平台客户 / 接入方）';

-- 24 调用方凭证（与 app_credential 同构）：AES-256-GCM 密文 + ACTIVE/ROTATING/RETIRED 轮换并存。
--    验签方向 ACTIVE + ROTATING 逐条试（任一命中即通过），与 app_credential 回调验签口径一致（M0-04 §3）。
CREATE TABLE client_credential (
    id             BIGINT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
    client_id      VARCHAR(32)  NOT NULL COMMENT '所属调用方',
    kind           VARCHAR(16)  NOT NULL COMMENT '凭证类型（须与 adapter.credentialKind() 一致）：API_KEY / HMAC_SECRET / BEARER_TOKEN / BASIC',
    credential     TEXT         NOT NULL COMMENT '凭证内容（AES-256-GCM 可逆加密；复合凭证 JSON 化）',
    status         VARCHAR(16)  NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE 当前使用 / ROTATING 轮换并存 / RETIRED 已失效',
    activated_at   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '生效时间',
    retired_at     DATETIME     COMMENT '失效时间',
    rotating_until DATETIME     COMMENT 'ROTATING 并存窗口截止（默认 +24h；过期后读取路径惰性视为 RETIRED）',
    created_at     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    KEY idx_client_credential (client_id, kind, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='调用方凭证（支持轮换并存）';

-- 25 接入鉴权审计（本设计的核心交付物之一）：记录「谁（主体 + 名称）· 从哪（IP）· 用什么方式 · 结果如何」。
--    写入点统一（网关切面 finally flush），数据来自 AccessAuthContext（ThreadLocal，与 CallLogContext 同构）；
--    PASS / REJECT 均记（可配只记失败）；异步批量写（AccessAuthWriter，复用 CallLogWriter 模式）。
CREATE TABLE access_auth_log (
    id              BIGINT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
    trace_id        VARCHAR(32)  COMMENT 'traceId（与 call_log / 运行表同源，可三方串联）',
    direction       VARCHAR(16)  NOT NULL COMMENT 'INBOUND_CALL 调用方→平台（Flow A）/ CALLBACK 供应商回调→平台（Flow B）',
    principal_type  VARCHAR(16)  NOT NULL COMMENT '主体类型：CLIENT 调用方 / SUPPLIER 供应商',
    principal_id    VARCHAR(32)  COMMENT '主体标识：client_id 或 app_id（主体未识别时为空，如未带 X-Client-Id 的强制模式）',
    principal_name  VARCHAR(64)  COMMENT '主体名称快照（主体改名 / 删除后历史仍可读——审计的硬要求）',
    interface_id    BIGINT       COMMENT '关联接口（多态引用不设外键；接口删除后仍保留）',
    interface_code  VARCHAR(16)  COMMENT '接口标识快照',
    auth_method     VARCHAR(24)  NOT NULL COMMENT '鉴权方式：API_KEY / HMAC-SHA256 / HMAC-SHA1 / HMAC-SHA512 / BEARER / BASIC / IP_WHITELIST / NONE',
    auth_adapter_id VARCHAR(16)  COMMENT '命中的鉴权适配器实例（adapter.id；平台内置或主体未知时为空）',
    result          VARCHAR(8)   NOT NULL COMMENT 'PASS 通过 / REJECT 拒绝',
    error_code      VARCHAR(16)  COMMENT '拒绝错误码（40100/40101/40103/40107/40108…）',
    reason          VARCHAR(255) COMMENT '结论说明（拒绝原因 / 通过补充，如「OPTIONAL 模式未带主体」）',
    client_ip       VARCHAR(45)  COMMENT '调用方来源 IP（IPv4/IPv6；解析口径见 §11）',
    xff_chain       VARCHAR(255) COMMENT '原始 X-Forwarded-For 头（取证用；未携带为空）',
    user_agent      VARCHAR(200) COMMENT 'User-Agent（截断 200，取证用）',
    latency_ms      BIGINT       COMMENT '鉴权耗时（毫秒，闸门判定区间；回调验签记链内验签区间）',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    KEY idx_auth_time (created_at),
    KEY idx_auth_principal (principal_id, created_at),
    KEY idx_auth_ip (client_ip, created_at),
    KEY idx_auth_result (result, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='接入鉴权审计';
```

### 4.2 删除策略（追加到 `schema.sql` 末尾的删除约定）

```
· 删调用方     → 级联删其凭证（client_credential）；**审计表不删**（access_auth_log 只靠保留期清理，
                 主体已删则靠 principal_id/principal_name 快照回溯）；存在引用 forbidden?（无引用方，允许删）
· 删鉴权适配器 → client_app.auth_adapter_id 置 NULL（回退「未配置」→ 启用后 fail-closed 40108）
                 —— 与「删适配器 → app.*_adapter_id 置 NULL」同规则，同一个 repository 方法扩展
· 删接口       → 不删 access_auth_log（interface_id/interface_code 快照保留）
· 审计表清理   → 保留期任务（默认 90 天）或手工 SQL；不进 worker（P2，§18）
```

### 4.3 为什么审计**必须**冗余名称快照

`principal_name` / `interface_code` 是**快照**而非外键 join 结果：审计表的价值在于
「三个月后还能回答**当时**是谁」。若靠 join，调用方改名或删除后，历史审计行会变成
「未知主体」——这正好抹掉了审计要保留的信息。同理 `client_ip` 与 UA 也是**事实记录**，不做解析归一。

---

## 5. 鉴权方式目录（方式矩阵）

### 5.1 四种 P1 方式（+ Noop 兼容）

| 方式 | 线协议（默认，头名可配） | 凭证 `kind` | 校验要点 | 审计 `auth_method` |
|---|---|---|---|---|
| **API Key** | `X-Client-Id: ERP-PROD`<br>`X-Api-Key: <secret>` | `API_KEY` | 常量时间比较；ACTIVE+ROTATING 逐条试 | `API_KEY` |
| **HMAC 签名** | `X-Client-Id`<br>`X-Timestamp: <秒级>`<br>`X-Signature: <hex>` | `HMAC_SECRET` | ①时间戳容差（默认 300s）②`Hex(HMAC(algo, secret, ts + "." + UTF-8(rawBody)))` ③防重放 | `HMAC-SHA256` / `HMAC-SHA1` / `HMAC-SHA512` |
| **Bearer Token** | `X-Client-Id`<br>`Authorization: Bearer <token>` | `BEARER_TOKEN` | 常量时间比较；`prefix` 可配（`Bearer` / `Token` / 空 = 裸 token） | `BEARER` |
| **仅 IP 名单** | 无需凭证头（**白名单必须非空**） | —— | 名单命中即通过；**无名单 + 无凭证 → 一律拒**（D-CA-7） | `IP_WHITELIST` |

**HMAC 签名串沿用既有口径**（`HmacSigner`：`timestamp + "." + rawBody`，hex 小写）——**不新造算法**：
调用方与供应商可以用**同一个签名工具 / 同一份联调脚本**，这是一条实打实的运维减法。复用
`HmacSigner.sign/verify`（含 `MessageDigest.isEqual` 常量时间比较），零新增密码学代码。

### 5.2 待确认项（P2/P3，不在本次）

Basic（`BASIC`）、云厂商签名（调用方是云厂商客户端的场景，可复用 `CloudSignatureAdapter` 思路）、
OAuth2 client_credentials（需 token 端点与缓存，成本高）、mTLS（需连接级配置 + 证书存储）。

### 5.3 适配器 impl 清单（追加进 `AdapterImplCatalog`）

| impl | 类型 | 名称（UI） | params（非密参数） | credentialKind |
|---|---|---|---|---|
| `ClientApiKeyVerifyAdapter` | auth | 调用方 API Key 验签 | `idHeaderName`(默认 `X-Client-Id`)、`credentialHeaderName`(默认 `X-Api-Key`) | `API_KEY` |
| `ClientHmacVerifyAdapter` | auth | 调用方 HMAC 验签 | `idHeaderName`、`signatureAlgorithm`、`signatureHeader`(默认 `X-Signature`)、`timestampHeader`(默认 `X-Timestamp`)、`timestampToleranceSeconds`、`replayProtection` | `HMAC_SECRET` |
| `ClientBearerVerifyAdapter` | auth | 调用方 Bearer 验签 | `idHeaderName`、`headerName`(默认 `Authorization`)、`prefix`(默认 `Bearer`，可置空 = 裸 token) | `BEARER_TOKEN` |
| `ClientIpWhitelistVerifyAdapter` | auth | 调用方 IP 名单 | `idHeaderName`（仅用于审计主体识别） | ——（不读凭证） |

> 同批 impl（回调验签方向，`INBOUND_AUTH` 复用同一批实现，避免两套代码）：`Client*VerifyAdapter`
> 的**校验逻辑与「是谁的凭证」无关**，因此**同一批适配器实例既可绑到 `client_app.auth_adapter_id`
> （调用方鉴权），也可绑到接口 `CALLBACK_AUTH` 角色（回调验签）**——见 §9.3。

---

## 6. 调用方鉴权流程（Flow A）详规

### 6.1 判定顺序（闸门内：步骤 ⓪ + 五步）

> **步骤 ⓪（先于一切）**：`OPTIONS` → 直接放行（CORS 预检不带凭证）；`X-Internal-Token` 命中
> → `PLATFORM_SELF` 放行（§3.4 / D-CA-17）。**以下 ①~⑤ 仅针对外部调用方。**

```
① 主体识别     X-Client-Id（可配头名）→ 查 client_app
                · 缺失 →（按 §6.2 真值表）
                · 不存在 / status=DISABLED → 40107「调用方不存在或已停用：xxx」
② IP 名单      黑名单命中 → 40103；白名单非空且未命中 → 40103（与 GatewayGuard 同口径、同错误码）
③ 方式解析     client_app.auth_adapter_id → adapter(type=auth, enabled=1)
                · 未配 / 适配器已停用 → 40108「调用方未配置可用的鉴权方式」
                · 适配器停用即拒（**不回退 Noop**——与链内「逐层回退到 Noop」语义相反，见 §6.5）
④ 凭证注入     client_credential(client_id, kind=adapter.credentialKind(), status ∈ {ACTIVE, ROTATING}
                且未过期) → CryptoService.decrypt → List<String>
                · 空列表 → 40108「调用方无可用凭证」
⑤ 适配器校验   adapter.process(ctx) → 通过 / BizException(40100 / 40101)
```

### 6.2 模式真值表（**实现照此写，测试照此测**）

| `mode` | `X-Client-Id` | 凭证头 | 结论 | `result` | `auth_method` |
|---|---|---|---|---|---|
| `OFF` | 任意 | 任意 | **跳过鉴权**（不校验、不报错） | `PASS` | `NONE`（reason：未启用调用方鉴权） |
| `OPTIONAL` | 缺失 | 无 | 放行（灰度观察流量） | `PASS` | `NONE`（reason：OPTIONAL 未带主体） |
| `OPTIONAL` | 缺失 | **有** | **拒 40107**（带了凭证却声明不出主体 = 配置错误或伪造，不能静默忽略） | `REJECT` | 按凭证头推断 |
| `OPTIONAL` | 有 | 无 | **拒 40100**（声明了主体却没凭证） | `REJECT` | 方式名（取适配器 method） |
| `OPTIONAL` | 有 | 有 | 走 ①→⑤ 完整校验 | `PASS`/`REJECT` | 方式名 |
| `ENFORCED` | 缺失 | 任意 | **拒 40107** | `REJECT` | `NONE` |
| `ENFORCED` | 有 | 任意 | 走 ①→⑤ 完整校验 | `PASS`/`REJECT` | 方式名 |
| 任意 | 有但未知 / `DISABLED` | 任意 | **拒 40107**（**显式声明的主体必须可识别**，与 mode 无关） | `REJECT` | —— |

> `OFF` 行仍写审计，是为了支持**观察期**：`mode=OFF + audit.enabled=true` = 「先看清谁在调，再决定卡谁」。
> 这是本设计推荐的灰度第一步（§16）。

### 6.3 敏感头名动态注册（B1 缺陷修正，硬要求）

```java
// SensitiveDataMasker 增加动态注册（构造/启动时 + 适配器保存时）
void registerSensitiveHeader(String headerName);   // 幂等，大小写不敏感
```
注册来源两处：
1. **`AdapterImplCatalog` 元数据**：字段 `kind=secret` 或字段名形如 `*HeaderName` / `signatureHeader` 的
   **取值**（需在适配器保存时解析 params 并注册——它同时满足既有 `headerName`/`signatureHeader` 可配的漏区）；
2. **配置项** `client-auth.mask-header-names`（逗号分隔，运维兜底）。
> 校验：新增测试 `SensitiveDataMaskerTest#自定义密钥头名_动态注册后脱敏`；
> **反证用例**：把动态注册注释掉 → 该测试必红（钉住「不可回退」）。

### 6.4 防重放与时间戳（仅 HMAC）

- 时间戳头缺失 / 非法 → `40100`；超容差 → `40101`（与 M3 回调验签完全一致，文案复用）。
- 防重放：`replayProtection=true` 时按 `(clientId, signature)` 在容差窗口 ×2 内**内存去重**；
  单实例口径；缓存有界（沿用 `HmacCallbackVerifyAdapter` 的 `MAX_REPLAY_CACHE=10_000` 与惰性清理策略）。
- **不引入 Redis / 分布式去重**（与熔断/限流同口径，v1.1）。

### 6.5 「停用即拒」vs「逐层回退」——两处语义必须相反（易错点）

| 场景 | 链内（`ChainEngine.resolveBound`） | 闸门（本设计） | 为什么相反 |
|---|---|---|---|
| 适配器停用 / 缺失 | **逐层回退**：接口绑定 → 应用默认 → 平台默认 `Noop`（+ `log.warn`） | **直接拒绝 40108**。**绝不回退 Noop** | 链内回退是「出站行为可降级」（Noop = 不加签名，功能仍可用，有告警）；闸门回退 Noop = **鉴权系统自动打开大门**，是安全事故。**这条必须写进代码注释**（否则后来者「为了对齐链内语义」而改成回退） |

### 6.6 错误信息粒度取舍（D-CA-12 的展开）

| 选项 | 优点 | 缺点 | 选择 |
|---|---|---|---|
| 统一「鉴权失败」泛化文案 | 防主体枚举 | 联调期无法自助定位（本项目已多次因「看不清上游/平台差异」耗费排查时间） | ✗ |
| **描述性 msg + 精确 `error_code` + 审计留原因** | 联调自助（`40107 调用方不存在：ERP-PROD`） | 轻微枚举信息泄露（可得「该 client_id 是否存在」） | ✓ |

缓解措施：连续失败告警（§14.2）+ 观察期先摸清合法流量（§16）。**这是明确取舍，不是遗漏。**

---

## 7. 管理面（端点 / 页面 / 角色）

### 7.1 端点清单（`/api/admin/clients/*`，全部 `${PREFIX}/clients` 前缀 → 自动受 `AdminAuthFilter` 保护）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/clients?keyword=&status=` | 列表（含凭证角标：`hasApiKeyCredential` / `hasHmacCredential` / `hasBearerCredential`，一次 IN 查询，沿用 `findActiveKinds` 的模式） |
| POST | `/clients` | 新建（`client_id` 3-32 位大写字母/数字/`-`/`_`，全库唯一） |
| GET | `/clients/{clientId}` | 详情（含凭证遮显列表） |
| PUT | `/clients/{clientId}` | 更新（名称 / 联系人 / `auth_adapter_id` / IP 名单 / 配额 / 描述） |
| POST | `/clients/{clientId}/enable` · `/disable` | 启停用（停用即拒 40107） |
| DELETE | `/clients/{clientId}` | 删除（级联删凭证；审计保留） |
| GET | `/clients/{clientId}/credentials` | 凭证遮显列表（尾 4 + `status` + `rotating_until`，永不回显明文） |
| POST | `/clients/{clientId}/credentials` | 新增 / 轮换（`prepare`，返回**一次性明文** + 指纹） |
| PUT | `/clients/{clientId}/credentials/{id}` | 更新凭证值 |
| POST | `/clients/{clientId}/credentials/{id}/activate` · `/retire` | 激活 / 失效（轮换激活与旧凭证退役） |
| DELETE | `/clients/{clientId}/credentials/{id}` | 删除 |
| GET | `/monitor/access-logs?principalId=&ip=&result=&method=&from=&to=&page=` | **接入鉴权审计**查询（时间窗强制 ≤7 天，未传按近 24h；与 `call-logs` 同纪律） |
| GET | `/monitor/access-logs/summary` | 近 24h 摘要：通过 / 拒绝数、Top 拒绝原因、未知主体计数（**观察期主力视图**） |

**凭证服务复用策略**：`CredentialService` 目前把 `appId` 硬绑在方法签名上（`prepare(appId, req)`）。
实现二选一（**待拍板 D-CA-15**，见 §18）：
- **A（推荐）**：抽 `CredentialStore` 接口（`ownerId` + `ownerKind` 参数化），`CredentialService` 与
  新增 `ClientCredentialService` 共用实现，**语义入口分开**（避免 `appId` 语义漂移到 client）；
- B：复制一份 `ClientCredentialService`（~150 行重复，但改动面最小、零 M0-04 回归风险）。
> 无论 A/B，**遮显口径、轮换语义（`countLiveRotating` 排除已过期）、`synchronized` 兜底、AES-GCM 加解密
> 必须完全一致**——这是 M0-04 与 E2 修复踩过的地方（`countByStatus` 与 `countLiveRotating` 的区别）。

### 7.2 页面

| 页面 | 内容 |
|---|---|
| 新增 `views/Clients.vue`（侧边栏「调用方管理」，路由 `/clients`，图标 `⊞`） | 列表（ID / 调用方标识 / 名称 / 鉴权方式 / IP 名单 / 状态 / 凭证角标）+ 新建·编辑弹窗（**内联凭证卡片**，复用 `CredentialEntry.vue`，显示条件 = 选了非 `ClientIpWhitelistVerifyAdapter` 的适配器）+ 启停用 / 删除 |
| `views/Monitor.vue` 新增 Tab「接入鉴权」 | 审计表：时间 / 主体（名称 + 标识）/ 类型（调用方·供应商）/ 接口 / **鉴权方式** / **来源 IP** / 结果 / 原因 / traceId；筛选（主体 / IP / 结果 / 方式 / 时间窗）+ 抽屉详情（含 `xff_chain`、UA）；复用 `PayloadViewer.vue` 不需要（审计无报文） |
| `utils/clients.mjs`（新） | 前端镜像：`clientIdValid()` / `authMethodLabel()` / `credentialKindOf(impl)`（与 `utils/roles.mjs` / `utils/users.mjs` 同模式）+ 单测 |

### 7.3 角色（零新增权限代码）

| 动作 | 需要的角色 | 由谁强制 |
|---|---|---|
| 查看调用方列表 / 审计 | 任意已登录（`VIEWER` 可） | `AdminAuthFilter`（只读放行） |
| 新建 / 编辑 / 启停用 / 删除 / 凭证操作 | `ADMIN` / `OWNER` | `AdminAuthFilter`（VIEWER 非 GET → `40302`）——**无需新增任何角色代码** |

> 纪律（CLAUDE.md 已记）：新增管理面写端点**不用改权限代码**；只有「新增角色相关入口」时才需同步
> `RoleRules` + `utils/roles.mjs` + 路由 `meta.roles`。本设计**不新增角色维度**。

---

## 8. 引擎侧改动（最小化，逐处清单）

| 文件 | 改动 | 说明 |
|---|---|---|
| `config/GatewayInboundAuth.java`（新） | 闸门：按 `if_type` 分派 + 登记 `AccessAuthContext` | 唯一新增的「入口级」类 |
| `config/ClientAuthProperties.java`（新） | `@ConfigurationProperties(client-auth.*)` | 与 `AuthProperties` 同模式 |
| `service/ClientAuthVerifier.java`（新） | ①②③④⑤ 五步判定（§6.1）+ 凭证注入 + 调适配器 | 纯逻辑，便于单测 |
| `engine/InboundAuthAdapter.java`（新，`Adapter` 子接口） | `method()` / `credentialKind()` | D-CA-6 |
| `engine/ChainEngine.java` | ① Flow A 的 `INBOUND_AUTH` 注释从「Noop 占位（范围外）」改为「由网关闸门统一处理（本设计 §3.2），此处保留 Noop 槽位」；② 新增 `injectCallbackCredential(ctx, appId)`（对称 `injectCredential`），供新通用验签适配器读 `ctx.attrs("inboundCredentials")` | **不改任何既有行为**（`CALLBACK_AUTH` 默认仍是 `HmacCallbackVerifyAdapter`） |
| `engine/InboundEngine.java` | 链执行处加 try/catch：成功 → `AccessAuthContext.pass(method, adapter)`；`BizException(401xx)` → `AccessAuthContext.reject(...)` | 5 行，M3 唯一改动点 |
| `service/GatewayGuard.java` | 抽 `checkQps/checkDailyQuota/checkIp` 为可复用（`Check` 入参从 `AppRow` 泛化为「限额三元组 + 名单」）——为 P2 的调用方维度预留；**P1 行为不变** | 保持 42901/42902 语义与 M4 `c8` 用例 |
| `controller/GatewayController.java` | ① 路由命中后调闸门；② 闸门通过时补 `CallLogContext.set(iface.id(), iface.appId(), traceId)` | ② 是**附带收益**：鉴权失败时 `call_log` IN 条也带得上接口/应用（原实现里 `CallLogContext` 只在引擎内设置，闸门拒绝的行 `interface_id` 恒空） |
| `aspect/CallLogAspect.java` | `finally` 中追加 `AccessAuthContext.flush()`（在 `CallLogContext.clear()` 旁） | 与 call_log 同一清理契约（**引擎设置、网关切面清理**） |
| `controller/admin/InterfaceController.java` | `testCallback` 自调网关时加 `X-Internal-Token` 头（值取内部令牌 Bean）—— **v1.1 新增改动面**（D-CA-17 / §3.4） | 不改则 `ENFORCED` 下「模拟回调」被自家闸门拒 |
| `controller/GatewayController.java`（或闸门 Bean） | **步骤 ⓪**：`OPTIONS` 直接放行 + 内部令牌判定（**常量时间比较**、**空令牌不豁免**） | 遗漏 `OPTIONS` ⇒ 浏览器直调全被预检拦死 |
| `worker/AccessAuthWriter.java`（新） | 异步批量写（队列 1000 / 批 50 / 1000ms / 满则丢弃 + `Counter` + `log.warn` / `@PreDestroy` 冲刷） | 逐行对齐 `CallLogWriter`（含「丢弃计数真自增」这条 2026-09-12 修复） |
| `repository/AccessAuthLogRepository.java`（新） | `insertBatch` + `findPaged(...)` + `summary(...)` | 位置绑定写法照 `CallLogRepository`（**列清单与序号必须对齐**） |

---

## 9. 回调验签（Flow B）统一口径

### 9.1 方式扩展（从「只有 HMAC」到「同批方式」）

| 现状 | 本设计 |
|---|---|
| `HmacCallbackVerifyAdapter`（唯一手段，且**直接查库**读 `app_credential`） | 接口 `CALLBACK_AUTH` 角色可绑 **`ClientApiKeyVerifyAdapter` / `ClientHmacVerifyAdapter` / `ClientBearerVerifyAdapter`**（通用批，凭证由 `ChainEngine.injectCallbackCredential` 注入）；`HmacCallbackVerifyAdapter` **保留可用**（存量配置零迁移） |

**凭证来源不变**：仍是 `app_credential(kind=CALLBACK)`（M0-04：回调验签凭证独立于出站签名凭证）。
**唯一差异是凭证由谁注入**——这正是 D-CA-6 的价值：**一套验签代码，两个维护入口**。

### 9.2 审计回填（`InboundEngine` 5 行）

```
① 网关闸门（INBOUND 分支）登记：direction=CALLBACK，principal=SUPPLIER/{app_id, app.name}，client_ip，UA
② 链内验签结果回填：
   成功 → AccessAuthContext.pass(method=adapter.method(), adapterId)
   BizException(401xx) → AccessAuthContext.reject(code, msg)
③ 非 401xx 的链失败（40001 映射失败等）→ 审计**保持 PASS**（鉴权确实通过了），
   由 call_log / 状态机负责；审计不承担业务失败记录（职责边界）
```

### 9.3 是否迁移 `HmacCallbackVerifyAdapter`（待拍板 D-CA-16）

| 选项 | 说明 |
|---|---|
| **A（推荐，P5 可选）** | 把 `HmacCallbackVerifyAdapter` 改写为「读注入凭证」的薄适配器，**impl 名与 params schema 完全不变**（存量 `adapter` 行零迁移，数据库不动）。收益：删掉一处「适配器查库」的反模式，两方向彻底同构 |
| B | 保留现状（两套验签代码并存，但零 M3 回归风险） |

---

## 10. 接入审计设计（`access_auth_log`）

### 10.1 写入路径（唯一入口，与 `call_log` 同构）

```
闸门 / 链内  →  AccessAuthContext.set(...)（ThreadLocal；不可变记录 + 可变结论）
网关切面 finally → AccessAuthContext.flush() → AccessAuthWriter.offer(entry) → 【异步批量】DB
```
- **清理契约**：**闸门/引擎登记、网关切面清理**（照 `CallLogContext` 的既有纪律，防止 ThreadLocal 泄漏）。
- **两个管理面调试端点的结局不同**（**v1.1 复核更正**）——
  · `POST /interfaces/{id}/test`（测试接口）→ **直调引擎、不经网关** ⇒ **不写审计**（与「不落 IN 条 `call_log`」口径一致）；
  · `POST /interfaces/{id}/test-callback`（模拟回调）→ **自调网关**（`POST http://localhost:{port}{platformPath}`）
    ⇒ **会走闸门** ⇒ 走 §3.4 的内部令牌豁免（`PLATFORM_SELF`），**写审计**。
    ⚠️ 初版把两者都写成"不经网关"是**事实错误**（把自调当成了直调）；照初版实现，`ENFORCED` 下
    「模拟回调」会被自家闸门拒掉。
- **未命中接口的路径**：默认**不写审计**（`record-unmatched=false`）——否则任何人扫路径都能往审计表灌数据；
  改为 `Counter{result=unknown_interface}` + 采样 `log.warn`（P2 可开）。

### 10.2 保留期与体积估算

| 项 | 值 |
|---|---|
| 每条审计行（除 `trace_id`/`reason` 外基本定长） | ~250–350 字节 |
| 假设量级 | 与 `call_log` IN 条**同量级**（每次入站鉴权 1 条） |
| 默认保留 | **90 天**（`access-auth.audit.retention-days`，独立于 `call_log` 的归档策略） |
| 降量开关 | `record-pass=false`（只记失败）→ 体积通常降一个数量级；**观察期必须为 true** |

### 10.3 查询与展示

- 端点：`GET /api/admin/monitor/access-logs`（分页 + 组合过滤；**时间窗强制 ≤7 天**，未传按近 24h，
  与 `call-logs` 关键字检索同一条纪律——防全表扫）。
- 前端：Monitor「接入鉴权」Tab（表 + 筛选 + 抽屉详情），Observation 摘要卡（通过/拒绝/未知主体）。
- **交叉检索**：审计行有 `trace_id` → 可直接跳到「调用日志」看该 trace 的 IN/OUT 报文
  （这是把「谁调的」与「调了什么」串起来的关键路径，§17 手动验收 C-3 会验证）。

---

## 11. 来源 IP 与可信代理

| 项 | 口径 |
|---|---|
| 取值 | 默认 `request.getRemoteAddr()`；`app.api-center.trust-xff=true` 时取 `X-Forwarded-For` **首值**（复用既有 `GatewayGuard.resolveClientIp`，**单一实现点**，不新写一份） |
| 记录 | `client_ip`（解析后，VARCHAR(45) 兼容 IPv6）+ `xff_chain`（**原始整串**，取证用） |
| 默认 | `trust-xff=false`（既有默认，防伪造 XFF 绕过白名单） |
| P2 | `client-auth.trusted-proxies` 可信代理列表（只有来自可信代理的 XFF 才采信）——`X-Forwarded-For` 在**直连暴露**场景下可任意伪造，这是 `trust-xff=true` 的已知风险，P2 收敛 |
| 前端无关 | 平台对外路径 `/{platformPath}` **不在 CORS 映射内**（`WebConfig` 只映射 `/api/**`）→ 调用方为服务端，鉴权头不受浏览器预检影响（**注意**：若未来允许浏览器直调平台接口，需显式加 CORS 映射 + `Access-Control-Allow-Headers` 放行 `X-Client-Id` 等头，否则预检失败且**无审计**） |

---

## 12. 与「接入层防护」（D-M4-6）的职责重划

### 12.1 职责表（改造后）

| 检查 | 维度 | 位置 | 错误码 | 进审计表 | 进 `call_log` | 落运行表 |
|---|---|---|---|---|---|---|
| **来源 IP 名单（调用方）** | 调用方 | **闸门（新）** | `40103` | ✅ | ✅ | ❌ |
| **调用方鉴权（主体 + 凭证）** | 调用方 | **闸门（新）** | `40100/40101/40107/40108` | ✅ | ✅ | ❌ |
| 来源 IP 名单（应用） | 应用（供应商） | `GatewayGuard`（既存） | `40103` | ❌（既存行为） | ✅ | ❌ |
| QPS 限流 | 应用（供应商） | `GatewayGuard`（既存） | `42901` | ❌ | ✅ | ❌ |
| 日配额 | 应用（供应商） | `GatewayGuard`（既存） | `42902` | ❌ | ✅ | ❌ |
| QPS / 日配额（调用方，**P2**） | 调用方 | `GatewayGuard` 扩展 | `42901/42902` | ❌ | ✅ | ❌ |

**顺序**：闸门（身份）→ 防护（配额）。理由：**未认证的请求不该消耗配额**，且配额按已认证主体计数更准确。
**叠加语义**：调用方维度与应用维度 **AND**（任一拒绝即拒），既不破坏既有配置，又提供更细粒度。

> 📌 **同一个 40103 有"两个维度"**（v1.1 补充）：**调用方维度**（`client_app`，闸门内）与
> **应用维度**（`app`，`GatewayGuard` 内）**AND 叠加**，任一拒绝即拒。因此 `msg` **必须能区分是哪张名单拦的**：
> `「调用方来源 IP 被拒（黑名单）：1.2.3.4」` / `「应用来源 IP 被拒（不在白名单）：1.2.3.4」`
> —— 同码不同文案，运维才能一眼定位（否则"被拒了但不知道是哪张名单"）。

- **两个管理面调试端点的结局不同**（**v1.1 复核更正**）——
  · `POST /interfaces/{id}/test`（测试接口）→ **直调引擎、不经网关** ⇒ **不写审计**（与「不落 IN 条 `call_log`」口径一致）；
  · `POST /interfaces/{id}/test-callback`（模拟回调）→ **自调网关**（`POST http://localhost:{port}{platformPath}`）
    ⇒ **会走闸门** ⇒ 走 §3.4 的内部令牌豁免（`PLATFORM_SELF`），**写审计**。
    ⚠️ 初版把两者都写成"不经网关"是**事实错误**（把自调当成了直调）；照初版实现，`ENFORCED` 下
    「模拟回调」会被自家闸门拒掉。

现状：`GatewayGuard.check(routed.app)` 用**供应商应用**的 `ip_whitelist` 校验**调用方** IP。三种可能意图：

| 选项 | 语义 | 影响 |
|---|---|---|
| **A（本次采纳）** | 「该应用**全部入站流量**的来源 IP」（含 OUTBOUND 的调用方 + INBOUND 的回调来源） | 行为不变（M4 `c8` 绿）；缺点是**一个供应商应用无法分别为「调用方」与「回调来源」配名单**→ 用调用方维度名单补（新表字段） |
| B | 拆列：`app.ip_whitelist` 只管回调来源，另加 `app.client_ip_whitelist` 管调用方 | 更精确，但要改 `app` 表 + DTO + 前端 + 迁移，且 M4 用例语义要复核 |
| C | 语义收紧：`app.ip_whitelist` **只对 INBOUND 生效** | 语义最干净，但**会改既有行为**（OUTBOUND 调用不再受应用名单约束）→ M4 `c8` 用的是 `qps_limit` 不受影响，但 `GatewayGuardTest#白名单非空未命中_40103` 的集成用法需复核 |

**建议**：本次取 **A**（零行为变更，靠新调用方维度补足精度）；
把 **B** 记入 backlog（若出现「同一供应商既回调用固定 IP、又有多个调用方」的真实诉求，再按 B 拆列）。
**评审时请明确表态**。

---

## 13. 可观测与告警

### 13.1 指标（Micrometer，命名沿用 `apicenter.*`）

| 指标 | 标签 | 说明 |
|---|---|---|
| `apicenter.gateway.auth` | `direction`(INBOUND_CALL/CALLBACK)、`principal`、`method`、`result`(pass/reject/unknown_interface) | 鉴权判定计数 |
| `apicenter.gateway.auth.latency` | `direction`、`method` | 鉴权耗时（P95/P99 百分位） |
| `apicenter.accessauthlog.dropped` | —— | 审计队列丢弃计数（**必须真自增**——2026-09-12 修过 `calllog.dropped` 恒 0 的同类 bug） |
| `apicenter.auth.unknown_principal` | `ip` | 未知主体探测（P2 用于封禁决策） |

### 13.2 告警（复用 `AlertService`）

- **连续鉴权失败**：按主体（`principal_id`，主体未知时按 `client_ip`）5 分钟窗口计数 ≥
  `client-auth.fail-alert-threshold`（默认 10）→ `alert_event`（`rule_id=NULL`，`metric=auth_fail_streak`，
  `level=CRITICAL`），文案含主体名 + IP + 最后错误码。**逐行照抄** `AlertService.recordVerifyFailure`
  的实现模式（含「命中即重置窗口」）。
- **验签连续失败**：既有 `metric=verify_fail_streak` **保留**（回调方向），两条并行不冲突
  （口径差异：一条按 app，一条按 principal）。
- **临时封禁**：**不做**（与 M4 结论一致，安全能力 v1.1）。

### 13.3 日志

- 闸门判定**单行结构化**：`鉴权结论 result=REJECT principal=ERP-PROD ip=1.2.3.4 method=API_KEY code=40100 reason=...`
  （便于 `grep` 与日志平台正则）。
- **禁止**打印凭证（明文 / 密文 / 摘要）与完整签名——只打尾 4 或指纹（`SensitiveDataMasker.maskValue` 口径）。

---

## 14. 配置项（追加到 `application.yaml`）

```yaml
app:
  api-center:
    client-auth:
      # 灰度三态：OFF 不校验（现状）/ OPTIONAL 有凭证就验、无凭证放行 / ENFORCED 必须通过
      # 推荐上线路径：OFF+审计（观察）→ OPTIONAL（灰度）→ ENFORCED（强制）
      mode: OFF
      # 主体标识头名（平台默认；单个调用方可在其适配器 params.idHeaderName 覆盖）
      id-header: X-Client-Id
      # 额外需脱敏的头名（兼容既有可配头名，见 §6.3 B1 缺陷）
      mask-header-names: ""
      audit:
        enabled: true          # 独立开关：OFF + audit=true = 「观察模式」（先看清流量）
        record-pass: true      # false = 只记拒绝（降量，但观察期必须 true）
        record-unmatched: false # 未命中接口的路径不写审计（防刷表）
        retention-days: 90     # 仅供人工/脚本清理参考（P1 不做自动清理任务）
      fail-alert-threshold: 10 # 5 分钟窗口内同主体连续失败达阈值 → alert_event
      # 注：**内部调用令牌（D-CA-17 / §3.4）无需配置项** —— 启动时 SecureRandom 生成、仅存内存、
      #     进程内比对；自调是本实例到自己，多实例各自独立即可（也避免把密钥写进仓库/配置）
    # 复用既有：trust-xff（默认 false，见 §11）
```

**新增 Beans**：`ClientAuthProperties`（`@EnableConfigurationProperties`）、`AccessAuthWriter`（`@Component`，
构造即启动消费线程——照 `CallLogWriter`；**注意**：集成测试要能拿到 `pending()` 做同步等待，
或提供 `flushNow()` 测试钩子，避免「断言早于异步写」。

---

## 15. 灰度与迁移（上线纪律）

| 步骤 | 配置 | 观察什么 | 通过标准 | 回退 |
|---|---|---|---|---|
| **S0 部署（不改行为）** | `mode=OFF`, `audit.enabled=true` | 「接入鉴权」Tab：谁在调、用什么方式、IP 分布、未带主体的量 | 「不带任何凭证头」的合法调用方清单已明确 | 关 `audit.enabled` |
| **S1 配置调用方** | `mode` 仍 `OFF` | 为每个调用方建 `client_app` + 凭证 + 适配器；给出口头/文档约定（`X-Client-Id` + 密钥） | 逐一对调用方**用 `/test` 之外的独立脚本**验证（用真实头调真实接口，审计行 `result=PASS, method=API_KEY`） | 删调用方 |
| **S2 灰度** | `mode=OPTIONAL` | 拒绝数（应为 0，或全是**配置错**的调用方）；`has X-Client-Id 但无凭证` 量 | 连续 1 天拒绝率 ≈ 0 | `mode=OFF` |
| **S3 强制** | `mode=ENFORCED` | 拒绝数、`unknown_principal`、告警 | 拒绝仅来自预期外来源 | `mode=OPTIONAL` → `OFF` |
| **S4 收尾** | 保留 `ENFORCED` | —— | 更新《使用教程》§9.1 调用速查（加鉴权头）+ FAQ | —— |

**硬要求**：`ENFORCED` 前必须**逐个调用方拿到「不带凭证的存量调用量 = 0」的证据**（S0 的审计数据）。
**禁止**跳过 S0/S2 直接 `ENFORCED`。

---

## 16. 测试方案

### 16.1 自动化（新增 ~34 例）

| 测试类 | 例数 | 覆盖 |
|---|---|---|
| `ClientAuthVerifierTest`（单测） | 12 | §6.2 真值表逐行；各方式通过/失败；轮换 ROTATING 可用；停用 40107；未配方式 40108（**fail-closed**）；适配器停用**不回退 Noop**（反证）；IP 名单黑/白 |
| `ClientHmacVerifyAdapterTest`（单测） | 6 | 签名口径与 `HmacSigner` 一致；时间戳缺失/非法/超容差；防重放第二次拒；自定义头名；`prefix` 置空无前导空格（**别踩 `BearerTokenAuthAdapter` 的老坑**） |
| `AccessAuthWriterTest`（单测） | 3 | 批量落库；队列满丢弃 + Counter 自增；停机冲刷 |
| `ClientServiceTest`（单测，Mockito） | 6 | CRUD；`client_id` 校验；停用；删除级联凭证；**引用适配器存在性校验**（引用完整性应用层保证）；列表凭证角标 |
| `SensitiveDataMaskerTest`（补） | 2 | 动态注册后脱敏；**反证**（注释掉注册必红） |
| `ClientAuthIntegrationTest`（`@SpringBootTest`，**默认配置**证明闸门真生效） | 14 | ①ENFORCED+无主体→401/40107+**`outbound_request` 零增量**+审计 REJECT ②ENFORCED+正确 API Key→200+审计 PASS（断言 `client_ip`/`principal_name`/`auth_method` **三字段非空**）③错密钥→40100 ④HMAC 超容差→40101 ⑤HMAC 防重放→40100 ⑥OPTIONAL+无凭证→放行（method=NONE）⑦OPTIONAL+有凭证无主体→40107 ⑧OFF→完全现状（裸调 200）⑨停用→40107 ⑩调用方 IP 名单→40103 ⑪回调验签→审计 `direction=CALLBACK`+`auth_method=HMAC-SHA256` ⑫审计查询过滤/分页/≤7 天窗口 ⑬未命中路径不写审计 ⑭`RecordPass=false` 只记失败 |
| `MonitorStatsIntegrationTest`（补） | 1 | 审计 summary 计数正确 |
| 前端 | 6 单测 + 1 SSR | `utils/clients.mjs`（clientId 校验 / method 标签 / impl→kind 映射）；`Clients.vue` SSR 冒烟（**注意** `localStorage` 顶层守卫 + `predictableProps`；`<script setup>` 里 computed 必须 `.value`） |

**必测「反证用例」**（本项目纪律：钉住「不可回退」的修复）：
1. **闸门拒绝 → `outbound_request` 零增量**（防后来者把闸门挪到 `createRecord` 之后）；
2. **审计行含 `client_ip` + `principal_name` + `auth_method` 三项非空**（用户诉求的直接断言，防「字段建了不填」）；
3. **适配器停用 → 40108 而非放行**（防「对齐链内语义」改成回退 Noop）；
4. **动态敏感头名注册**（防写死清单回退）。

**v1.1 追加 4 例（内部路径与豁免，对应 §3.4 / D-CA-17）**：

| # | 用例 | 断言 |
|---|---|---|
| 35 | `test-callback` 自调（带内部令牌）在 `ENFORCED` 下 | 网关**放行**、ack 正常返回；审计表新增 1 行且 `auth_method=PLATFORM_SELF` |
| 36 | 自调**不带**令牌 | 被拒 `40107`（证明确实过了闸门，豁免不是“路径白名单”） |
| 37 | `OPTIONS` 预检 | 放行（不要求 `X-Client-Id`） |
| 38 | 内部令牌配置为**空** | **不豁免**（防止“空令牌放行”被误实现） |

> 反证要求：用例 35 在**去掉豁免**后必须**转红**（否则说明它没真正经过闸门，测的是假的）。

### 16.2 集成测试隔离纪律（**照 CLAUDE.md 硬要求，漏一项就偶发红**）

新 `@SpringBootTest` 类必须同时满足：

```java
@SpringBootTest(properties = {
    // ① 直连管理面 HTTP 的类（审计查询 / 调用方 CRUD）→ 关管理面鉴权（认证本身由 AuthIntegrationTest 覆盖）
    "app.api-center.auth.enabled=false",
    // ② 四个 worker 属性一起置 1h（防启动即 scan() 与用例抢跑 → 全量套件下偶发红，单跑永远绿）
    "app.api-center.retry-worker-fixed-delay-ms=3600000",
    "app.api-center.retry-worker-initial-delay-ms=3600000",
    "app.api-center.alert-worker-fixed-delay-ms=3600000",
    "app.api-center.alert-worker-initial-delay-ms=3600000"
})
```
**③ `AccessAuthWriter` 异步** → 断言前要等队列排空（提供 `flushNow()` 或轮询 `pending()==0` + 短超时），
否则会出现「断言早于落库」的假红（与 `CallLogWriter` 同类坑）。

### 16.3 手动验收（约 25 分钟）

| 阶段 | 步骤 | 判据 |
|---|---|---|
| **一 观察模式** | `mode=OFF` + `audit.enabled=true` → 裸调一个出站接口 → 看「接入鉴权」Tab | 出现 `PASS / method=NONE / 来源 IP = 本机`；主体名为空（未带主体） |
| **二 配置与灰度** | 建调用方 `DEMO-CLIENT` + API Key 适配器 + 凭证 → `mode=OPTIONAL` → ①带正确头 ②带错头 ③不带任何头 | ①200 + 审计 `PASS/API_KEY/主体名=DEMO-CLIENT/IP` ②401 + 审计 `REJECT/40100` ③200（OPTIONAL）+ 审计 `PASS/NONE` |
| **三 强制与联动** | `mode=ENFORCED` → ①裸调 ②正确头调 ③HMAC 调用方（另一个）调 ④改 `run` 后裸调 ⑤监控页按 IP/结果筛选 ⑥点审计行的 traceId 跳「调用日志」 | ①401/40107 且**运行记录页零新增**②200 ③200 ④401/40100 ⑤筛选结果一致 ⑥能看到该 trace 的 IN/OUT 报文（**「谁调的」↔「调了什么」串起来**） |
| **四 回调侧** | `/test-callback` 之外用 curl 打一个 `INBOUND` 接口（正确签名 / 错签名） | 审计出现 `direction=CALLBACK` + `auth_method=HMAC-SHA256` + `principal_type=SUPPLIER` + 应用名 |

**验收速查 curl**（写进《使用教程》§9.1）：

```bash
# 调用方（Flow A）——新增鉴权头（API Key 方式）
curl -X POST http://localhost:8080/<平台侧路径> \
  -H 'Content-Type: application/json' \
  -H 'X-Client-Id: DEMO-CLIENT' \
  -H 'X-Api-Key: <凭证明文（新增凭证时只返回一次）>' \
  -H 'X-Biz-Id: <业务键>' \
  -d '<请求体>'

# 调用方（HMAC 方式）：sig = hex(HMAC-SHA256(secret, "<ts>." + body))，与供应商回调同一口径
curl -X POST http://localhost:8080/<平台侧路径> \
  -H 'X-Client-Id: DEMO-CLIENT' -H 'X-Timestamp: 1758230400' -H 'X-Signature: <sig>' \
  -d '<请求体>'

# 行为开关（应急回退）
curl -X POST http://localhost:8080/api/admin/clients/DEMO-CLIENT/disable -H "Authorization: Bearer <token>"
# 审计查询
curl -H "Authorization: Bearer <token>" \
  'http://localhost:8080/api/admin/monitor/access-logs?result=REJECT&from=2026-09-19&to=2026-09-20'
```

---

## 17. 改动面清单（文件级，供评审逐条确认）

**后端新增（11）**：`config/GatewayInboundAuth`、`config/ClientAuthProperties`、
`service/ClientAuthVerifier`、`service/ClientService`、`service/ClientCredentialService`（或 §7.1 A 方案）、
`engine/InboundAuthAdapter`、`aspect/AccessAuthContext`、`worker/AccessAuthWriter`、
`repository/ClientRepository`、`repository/ClientCredentialRepository`、`repository/AccessAuthLogRepository`、
`controller/admin/ClientController`、`adapter/auth/Client{ApiKey,Hmac,Bearer,IpWhitelist}VerifyAdapter`（4）、
`model/{ClientRow,ClientCredentialRow,AccessAuthLogRow}`、`dto/ClientDtos`、`dto/AccessAuthDtos`
（实际约 20 个文件）。

**后端修改（8，均小改）**：`controller/GatewayController`、`engine/ChainEngine`、`engine/InboundEngine`、
`service/GatewayGuard`（抽方法，P1 行为不变）、`service/AdapterImplCatalog`（+4 impl）、
`aspect/CallLogAspect`（finally flush）、`aspect/SensitiveDataMasker`（动态注册）、
`controller/admin/MonitorController`（+2 端点）、`repository/CredentialRepository`（+1 解绑方法，若走 A 方案）。

**前端**：+ `views/Clients.vue`、`utils/clients.mjs`（+单测）、Monitor 加 Tab；改 `router/index.js`、
`layout/MainLayout.vue`（菜单）。

**文档同步（必须，8 处）**：

| 文档 | 同步内容 |
|---|---|
| `doc/schema.sql` | 3 张新表（第 23/24/25 张）+ 删除约定 + 迁移块注释（`✅ 已应用到开发库`） |
| `doc/表结构设计.html` | 表计数 22 → **25**；新增三表结构；枚举汇总加 `auth_method` / `principal_type` / `direction` / `client_credential.kind` |
| `doc/API中心设计方案.md` | **§1.2**（凭证两类 → 三类主体说明）、**§3.1**（调用方鉴权从「平台统一处理，不在模型内」→「网关闸门已实现」）、**§5.3**（入站鉴权方式目录扩展）、**§6.2 错误码表**（+40107/40108） |
| `doc/API中心时序图与流程图.md` | §二 出站时序：`入站鉴权（组件范围外，Noop 占位）` → `入站鉴权（调用方鉴权，网关闸门）`；删图二注的「不在本组件模型内」 |
| `doc/API中心项目说明.md` | §2 边界「**不做调用方鉴权**」→ 修订为「做鉴权、不做授权」 |
| `doc/API中心使用教程.md` | §9.1 调用速查加鉴权头；新增 §「调用方管理」；FAQ 加鉴权失败三步取证 |
| `doc/reset-dev.sql` | 运行类新增 `TRUNCATE access_auth_log`；配置类新增 `TRUNCATE client_credential; TRUNCATE client_app;`（按依赖顺序，凭证先于主体） |
| `CLAUDE.md` | 表计数 22 → 25；新增 Gotchas（**适配器停用不回退 Noop**、审计异步写要在断言前等排空、敏感头名动态注册、`access_auth_log` 三处列清单对齐） |
| `doc/开发文档/整体测试方案.md` | 用例库新增 A 组（接入鉴权）+ 回归矩阵 R7；§1.5 黄金链路加「带鉴权头调用」 |

**seed**：`SeedDataInitializer` 追加一个演示调用方（`client_app: DEMO-CLIENT` + `API_KEY` 凭证 +
`ClientApiKeyVerifyAdapter` 实例）——**但 `seed.enabled=false` 默认不导入**，且**不影响既有 fastmoss 黄金用例**
（新增行，不改既有行；注意 2026-09-18 的教训：不要给 seed 资产写死库态断言）。

---

## 18. 待拍板项汇总（评审请逐条表态）

| # | 待拍板 | 选项 | 建议 |
|---|---|---|---|
| **Q1** | D-CA-11：应用级 IP 名单对 OUTBOUND 的语义 | A 保持（全量入站来源）/ B 拆 `app.client_ip_whitelist` / C 只对 INBOUND 生效 | **A**（零行为变更，用调用方维度补精度）；B 记 backlog |
| **Q2** | D-CA-15：凭证服务复用方式 | A 抽 `CredentialStore`（`ownerId` 参数化，语义入口分开）/ B 复制一份 `ClientCredentialService` | **A**（避免 150 行重复），但需回归 M0-04 全部凭证用例 |
| **Q3** | D-CA-16：`HmacCallbackVerifyAdapter` 是否迁移到统一契约 | A P5 迁移（impl 名不变，DB 零迁移）/ B 保留两套 | **A**（P5，独立批次，不动 P1 关键路径） |
| **Q4** | 审计保留期与自动清理 | 90 天 + P1 手工 / 90 天 + P1 加清理任务 | P1 **手工**（不为一张新表先引调度），P2 加任务 |
| **Q5** | 调用方 QPS / 日配额是否 P1 | 是 / P2 | **P2**（`GatewayGuard` 泛化有回归面，别和鉴权耦合在一个批次） |
| **Q6** | 是否允许「无 `X-Client-Id`，按密钥摘要反查主体」 | 允许（单头更省事）/ 不允许 | **不允许**（P1 保持 `X-Client-Id` 必带；P2 若调用方强烈要求再评估） |
| **Q7** | 调用方 ACL（授权范围：能调哪些接口） | P2 / 不做 | **P2**（鉴权 ≠ 授权；但多租户场景迟早要，先记录） |
| **Q8** | 审计是否记报文摘要（`payload_sha256`，证明「签了什么」） | 记 / 不记 | **P2**（多一次全量哈希，量级不划算） |
| **Q9**（v1.1 新增） | 内部调用豁免的**载体** | 内部令牌（推荐） / 路径白名单 / 平台自建 client 身份 | **内部令牌**（`SecureRandom` 启动生成、进程内比对；不用 IP、不进配置） |

---

## 19. 落地计划（批次 + 人日 + 出口标志）

| 批次 | 内容 | 人日 | 出口标志 |
|---|---|---|---|
| ~~**B1 数据与目录**~~ **✅ 已落地（2026-09-23）** | DDL（3 表）+ `ClientAppRepository` + `ClientService` + `CredentialOwner`/`CredentialRepository` 参数化 + **抽 `CredentialStore`**（Q2 定案 A）+ `ClientCredentialService` + 端点 + 集成测试 | 3.0 | ✅ 调用方可 CRUD、可建/轮换凭证；凭证遮显不回明文；`client_app` 停用生效（`ClientAuthIntegrationTest` 9 例） |
| ~~**B2 鉴权内核**~~ **✅ 已落地（2026-09-23）** | `InboundAuthAdapter` 契约 + 4 个适配器 impl + `ClientAuthVerifier`（§6.1/§6.2）+ `AccessAuthContext` + `SensitiveDataMasker` 动态注册 + 单测 | 3.5 | ✅ §6.2 真值表逐行绿（23 例）；✅ HMAC 与回调口径一致（同一签名双实现均通过）；✅ 反证用例（不回退 Noop → 40108）绿 |
| **B3 入口与审计** | `GatewayInboundAuth` + `GatewayController` 接入 + `AccessAuthWriter` + `AccessAuthLogRepository` + 网关切面 flush + `InboundEngine` 审计回填 + 集成测试 | 3.0 | ENFORCED 下裸调 401 且运行表零增量；审计三字段（IP/名称/方式）非空；回调审计 `direction=CALLBACK` |
| **B4 管理面与可观测** | 端点 + `Clients.vue` + Monitor「接入鉴权」Tab + 指标 + 告警 + 前端单测/SSR | 3.0 | 页面上能建调用方、能查审计、能按 IP/结果筛；告警可触发 |
| **B5 文档与验收** | §17 的 8 处文档同步 + seed 演示调用方 + 手动验收（§16.3）+ 灰度手册 | 1.5 | 全库测试绿（预计 290 → ~325）；手动验收四阶段全过 |
| **B6（可选，P5）** | Q3：`HmacCallbackVerifyAdapter` 迁移到统一契约 | 1.0 | 存量回调验签用例全绿（DB 零迁移） |
| — | **合计** | **15.0 人日**（含可选批次 1.0） | —— |

**依赖与顺序**：B1 → B2 → B3（B4 可与 B3 后半并行）。**B3 是唯一碰热路径的批次**，压测口径见
《M5压测报告.md》——**鉴权闸门新增一次凭证解密 + 一次常量时间比较**（每请求），
`mode=OFF` 时**零开销**（早退）；`ENFORCED` 下建议在 B3 出口跑一次 M5 五场景中的「吞吐 / P99」两场景做偏差确认。

---

## 20. 坑预判（写实现前先读，避免重蹈覆辙）

| # | 预判坑 | 规避 |
|---|---|---|
| 1 | **适配器停用回退 Noop**（「对齐链内语义」的诱惑） | 闸门内**只认拒绝**（40108）；代码注释 + 反证用例双保险（§6.5） |
| 2 | **可配头名不脱敏** → 密钥明文落 `call_log.req_headers` | 动态注册（§6.3）+ 反证用例；`X-Api-Key` 之外的头名一律走注册 |
| 3 | **`prefix` 置空产生前导空格**（`BearerTokenAuthAdapter` 在 2026-09-18 踩过：`" token"` 被判无效） | 复用修好的实现思路：空 prefix ⇒ **裸 token 不拼空格**，并写单测 |
| 4 | **审计异步写与断言竞态** | `flushNow()` 测试钩子或轮询 `pending()==0`（§16.2 ③） |
| 5 | **`AccessAuthLogRepository.insertBatch` 列清单与位置绑定序号错位** → **静默写错列**（不报错） | 照 `CallLogRepository` 写法 + 集成测试断言**每个字段的实际值**（尤其 `client_ip` / `auth_method`） |
| 6 | **`mode=OFF` 时忘记早退**，ENFORCED 逻辑跑一半（如仍解析 `X-Client-Id`）→ 现状调用被误拒 | 真值表第一行显式单测（OFF + 任意头 → 200） |
| 7 | **`CryptoService.decrypt` 单条失败让整次鉴权 500** | 逐条 try/catch `continue`（照 `HmacCallbackVerifyAdapter` 的既有写法），全失败才判不匹配 |
| 8 | **ROTATING 已过期仍被接受** | 用「排除 `rotating_until` 已过期」的读取（同 `countLiveRotating` 的 E2 修复口径），别用 `countByStatus` 类的粗口径 |
| 9 | **ThreadLocal 泄漏**（`AccessAuthContext` 未清）→ 串行请求带上别人的主体 | 与 `CallLogContext` 同一纪律：**闸门/引擎登记、网关切面 finally 清理**；调试端点各自 finally 清 |
| 10 | **新测试类漏置 worker 属性/`auth.enabled`** → 全量套件下偶发红（单跑永远绿） | §16.2 模板，照抄五条属性（2026-09-18 血的教训） |
| 11 | **给 seed 资产写死库态断言** | 新增行不写死；断言语义（如「至少 1 个调用方」），需要钉死就测试自建夹具（2026-09-18 同类事故） |
| 12 | **`DATETIME(0)` 四舍五入**（审计时间列） | 时间列写入统一走 `SqlTimes.ts(...)`；**不涉及调度时间**（审计无 `next_retry_at`），但 `rotating_until` 计算要留意 |
| 13 | 浏览器直调平台接口 → 预检失败且**无审计** | 平台对外路径不在 CORS 映射内（§11）；若要支持，显式加 CORS + `Allow-Headers`，否则会「静默查不到」 |

---

## 21. 附：与既有设计/表的关系一览（评审速查）
10. **自家闸门把自家工具打死**（v1.1 新增）：闸门在 `GatewayController` 内 ⇒ **平台自调也会过闸门**；
    最典型的是管理面「模拟回调」（`/test-callback` → 自调网关）：`ENFORCED` 一上线，该工具立刻 401。
    照 §3.4 的清单**逐条过一遍**（自调 / `OPTIONS` / 前置编排 / worker），漏一个就是一次线上事故。
11. **用「回环 IP」当豁免判据 = 开后门**（v1.1 新增）：反代 / 容器 / K8s 场景下 `client_ip` 的取法见 §11；
    一旦按 IP 豁免，外部流量只要能构造出回环来源就会**完全绕过鉴权**。→ 只认**内部令牌**（且空令牌不豁免）。


```
既有 22 张表                                    本设计
├─ app（应用=供应商）          ──── 对称参考 ──→ client_app（调用方=平台客户）        【新 23】
├─ app_credential（两类凭证）  ──── 同构   ──→ client_credential（调用方凭证）       【新 24】
├─ adapter（三类适配器）       ──── 复用   ──→ +4 个 auth impl（调用方/回调通用验签）
├─ call_log（调用日志）        ──── 分工   ──→ access_auth_log（鉴权审计）            【新 25】
├─ admin_user/admin_session    ──── 不涉及（管理面鉴权独立）
├─ interface/interface_*       ──── 不涉及（调用方鉴权不进接口模型 → 无快照/版本影响）
└─ outbound_request/…          ──── 不涉及（鉴权失败不落运行表）
```

**一句话总结**：新增**一个主体（调用方）+ 两个凭据/审计表 + 一个入口闸门**，把
「**谁在调平台 / 谁在回调平台 · 从哪来 · 用什么方式 · 结果如何**」变成**可配置、可灰度、可查询**的事实；
**不碰**接口模型、不碰状态机、不碰 M3 已验证路径、不改 M4 既有行为（Q1=A 时）。

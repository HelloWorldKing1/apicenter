# API 中心整体测试方案（总纲）

> 版本：v1.3（2026-09-07 三轮复查）｜状态：待评审定稿
> 变更记录：v1.3（三轮复查）——F1 标题 Body 类型文案对齐 UI（json 按钮，非 raw）；§7 统计卡补今日成功率口径（终态分母 SUCCESS+DEAD_LETTER、无终态 100）；§3 WireMock 轮次说明含 R5；R2/R4 增外网不可达时本地 m4 stub 等价兑底提示。v1.2（二轮复查）——修正 callback-allow-private 默认 true 致 SSRF 负向断言必失败（F8 改按开关状态断言）；§6 补应用标识 M3DEMO；B6 防重放拒绝码精确为 40100。v1.1（首轮评审修订）——F1 ② 4xx 本地自注册 401 stub；F4 重放语义按代码精确化；新增 F7/F8；§7 脱敏与 Prometheus 指标名校准；新增 §9.5 边界清单。
> 范围：M0–M5 全部能力（配置面 + Flow A 出站 + Flow B 入站 + 可观测容错 + 版本灰度 + 压测）。
> 定位：**总纲/执行入口**。每个场景给出「为什么测（原理）→ 怎么配（填什么/选什么）→ 怎么验（调用与查库）→ 出问题去哪查」；
> 各里程碑的专项细化（stub 场景、逐字段表、勾选式预期）见分册《M2/M3/M4/M5手动验收测试方案.md》，本纲中标注「详见分册」处执行时翻对应分册。
> 事实基准：自动化测试全库 174 个（2026-09-07 surefire 全绿）；本方案操作内容与 UI 实际字段、后端行为一致，若与实测不符以代码为准并回改本纲。

---

## 1. 为什么需要这套测试（测试目标与原理）

| 层 | 被测什么 | 回答什么问题 | 手段 |
|---|---|---|---|
| 配置面 | 应用/分组/接口/适配器/凭证 5 类配置的 CRUD、状态机、互斥校验、下拉联动 | 配得对、存得对、错配拦得住 | 手动 + M1 自动化 |
| 链路正确性 | 两核心链路（出站 Flow A / 入站 Flow B）端到端 | 「配置 → 链执行 → 真实收发 → 状态机」整条通路通不通 | 手动 + M2/M3/M4/M5 集成测试 |
| 语义正确性 | 字段映射 6 操作、协议 JSON/XML、信封剥壳、RESP 白名单、ACK 回执 | 报文对不对、字段转得准不准 | MappingEngine/Xml/Envelope 单测 + 端到端 |
| 容错 | 状态机全分支：4xx 死信 / 5xx 补偿 / 超时 UNKNOWN / 熔断 / 限流 | 上游乱来系统不乱、账目可对 | 手动 + M4 自动化 |
| 可观测 | call_log 双向、脱敏、traceId 贯穿、指标、告警、对账/死信面板 | 出事查得见、查得全、能收口 | 手动 + Monitor/Dashboard |
| 版本灰度 | 快照/回滚/灰度路由/事件即时失效/留痕 | 变更可追溯、回滚可用、灰度即时生效 | 手动 + M5 自动化 |
| 性能稳定性 | 吞吐、P99、异步写零丢弃、补偿批量扫描 | 线上扛得住、不拖垮主链路 | wrk 压测（D-M5-3） |
| 安全健壮 | XXE、SSRF、报文大小、脱敏、签名验签、乐观锁 | 不裸奔、不漏密钥、不脏数据 | 自动化 + 手动抽查 |

**贯穿全项目的三条原则（来自设计定稿，测试预期都以它为据）：**
1. **链失败与防护拒绝不污染状态机**（M0-01 D7 / D-M4-6）：解码/映射/编码/验签失败、QPS/日配额/IP 拒绝 → 只落 call_log，不写 outbound_request / inbound_delivery。验证「有/无记录」是常见断言点。
2. **收到即回 ack、与送达解耦**（§5.5/§6.1）：入站送达失败也回 ack 给供应商，由补偿 worker 重送——预期里「ack 成功但 delivery PENDING」是正常态，不是 bug。
3. **失败分类器口径**：5xx/429 → 短重试耗尽 → 补偿（COMPENSATING）→ worker 重放；4xx（非 429）→ 死信不重试；读超时/连接异常 → UNKNOWN 对账。

---

## 2. 被测系统功能地图（测试对象）

```
管理面 REST（/api/admin）：应用 · 分组 · 接口 · 适配器 · 凭证 · 监控（Monitor 含对账/死信/告警）
接入层路由：/actuator/health · 平台接口路径（/xxx，GatewayGuard 鉴权 + 限流 + IP 名单）
Flow A（出站）：调用方 → 入站鉴权(平台) → 协议解码 → 报文适配 → 字段映射 → 协议编码
              → 出站鉴权(供应商签名) → 调供应商 → 响应解码 + 信封剥壳 + RESP 白名单 → 回调用方
Flow B（入站）：供应商回调 → 回调验签(凭证独立) → 链适配 → 落 PENDING → 同步送达回调地址
              → 回 ack（解耦）→ 失败 worker 重送（callback_url_snapshot 快照）
容错与运维：熔断器三态 · UNKNOWN 对账（人工/TTL）· 死信重放 · 告警 · 指标 · 补偿 worker
M5：版本快照/回滚 · 灰度绑定（version 矩阵 #1-#4）· 链缓存事件失效 · test 端点 chainTrace
```

| 里程碑 | 交付能力 | 主要自动化覆盖（分册/类名） |
|---|---|---|
| M1 | 配置面五模块 + 状态机 + 乐观锁 | 22：M1IntegrationTest 14 · CryptoServiceTest 7 · 启动 1 |
| M2 | 链引擎 + 映射 6 操作 + 客户端 + 出站状态机 + 补偿 worker | 23：M2IntegrationTest 6 · MappingEngineTest 14 · JsonProtocolAdapterTest 3 |
| M3 | XML 编解码 + 入站回调 + HMAC 验签 + RESP/ACK + 送达快照 | 62：M3IntegrationTest 13 · XmlProtocolAdapterTest 13 · HmacCallbackVerifyAdapterTest 10 · InboundEngineTest 11 · RespFieldFilterTest 8 · AckRendererTest 3 · CallbackUrlValidatorTest 4 |
| M4 | 熔断 + 对账/TTL + 死信重放 + 网关防护 + 脱敏/traceId + 指标告警 | 57：M4IntegrationTest 7 · CircuitBreakerTest 11 · MonitorServiceTest 11 · AlertServiceTest 9 · GatewayGuardTest 9 · SensitiveDataMaskerTest 10 |
| M5 | 版本快照/回滚 + 灰度 + 事件失效 + chainTrace | 10：M5IntegrationTest 7 · SnapshotSerializerTest 3 |

---

## 3. 环境与数据（全部手动测试都在此拓扑执行）

| 项 | 值 | 说明 |
|---|---|---|
| 后端 | `http://localhost:8080`（`mvn spring-boot:run`） | 启动即幂等导入 seed（fastmoss 黄金用例 + M3 入站用例 IF-FM-CB-001），`app.api-center.seed.enabled` 可关 |
| 前端 | `http://localhost:5173`（`cd frontend && npm run dev`） | /api 代理到 8080 |
| 模拟上游/送达 | 本地 WireMock `:18080` | `java -jar ~/.m2/repository/org/wiremock/wiremock-standalone/3.9.1/wiremock-standalone-3.9.1.jar --port 18080 --root-dir src/test/resources/<m3|m4>-manual-stubs`；**按验收轮次切目录**：R3 用 `m3-manual-stubs`；R4、R5（M5 分册）与压测均用 `m4-manual-stubs`（切换需重启 WireMock）；M2 分册用 httpbin.org，不占本地端口 |
| 数据库 | 远程 PolarDB（`application.yaml` 配置；开发实例，白名单已通），库名 `apicenter` | **手动验收期间勿跑 `mvn test`**（WireMock 18080 冲突）；每 SQL ~百毫秒 WAN 往返 → 压测绝对值不作 KPI |
| 工具 | curl · jq · Navicat / mysql cli · wrk（压测） | |

**启动自检（每次验收前 1 分钟）：**
```bash
curl -s http://localhost:8080/actuator/health                      # UP
curl -s http://localhost:5173 | head -c 100                        # HTML
curl -s -X POST http://localhost:18080/echo-ok -d '{}'             # {"ok":true}（m4 stub）
curl -s -X POST http://localhost:18080/xml-supplier -d '{}'        # XML（m3 stub）
```

**数据与用例隔离规则（防止用例互相污染）：**
- 每个验收用独立「应用标识」命名空间：M2 手册用 `HTTPBIN`、M3 用 `M3DEMO`、M4 用 `M4DEMO`、M5 用 `M5DEMO`——跑分册时沿用各自命名，勿撞名；
- 列表接口不带子表（params/mappings 等恒空），断言子表必须走「详情」；
- 集成测试类自清自建（M5 先清后建），但**手动造数（死信/UNKNOWN/补偿）是验收遗留**，按第 10 节清理 SQL 收尾；
- WireMock 请求计数跨会话累积：需要「未匹配/零请求」断言前先 `POST http://localhost:18080/__admin/scenarios/reset` 重置 scenario 状态，计数类断言在测试类内由 resetAll 保证（手动场景少用计数断言）。

---

## 4. 管理面配置用例库（M1——五模块增删改查 + 状态机 + 凭证轮换）

> 每个场景：**目的** = 验证什么业务规则；**操作** = 填什么/选什么；**预期**。查库 SQL 统一见 §11.2。

### C1 应用 CRUD 与字段口径
- 目的：应用字段落库、状态机 DRAFT→ENABLED→DISABLED→CANCELLED、停用即拒请求。
- 操作：应用管理 →「＋ 新建」：
  - 应用标识 `TESTAPP01`（规则：字母/数字/`_`/`-` 1~32，填 `中文` 应被拒）、名称 `测试供应商`、服务地址 `http://localhost:18080`（**本地 WireMock 根地址**，别填 8080 自己）、供应商签名适配器/回调验签/默认报文三下拉**留空**（= Noop/继承语义）、QPS 限流与日配额留 0（不限）、IP 白名单/黑名单留空、描述随意 → 保存 → 状态「草稿」；
  - 行操作「启用」→「已启用」→ 直接 curl 平台路径应 200（网关放行）→「停用」→ 同一 curl 应返回 40102「应用已停用」→ 验证停用即拒；「启用」恢复。
- 预期：列表/详情字段与填一致；停用后拒绝且**不落运行表**（§11.2 SQL-1 查无新行）。
- 常见：标识重复/非法标识被拒；停用后忘了启用 → 后续链路全 40102（先查应用状态）。

### C2 凭证轮换（M0-04 定稿，出站与回调两类 kind 独立）
- 目的：ACTIVE→ROTATING（并存 24h 演示）→ACTIVE 或 RETIRED 的迁移、验签并存期间新旧都收。
- 操作：应用管理 → 点应用行 → 详情「凭证」区 →「更新凭证（供应商已换密钥）」：
  - 凭证类型选 **出站签名**（OUTBOUND）→ 凭证内容填 `token-v1` → 确认 → 出现 ACTIVE（尾 4 位指纹 v1）；
  - 再「更新凭证」填 `token-v2` → 出现 **ROTATING（旧并存）**；此时 Fingerprint 显示 v1 ACTIVE + v2 ROTATING；
  - 「激活」v2 → 旧的转 RETIRED → 出站签名切换为 v2（WireMock 看 Authorization 头值变化可证）；
  - 独立验证**回调验签凭证**：凭证类型选 **回调验签**（CALLBACK）填 `cb-secret` —— 与出站互不影响；
  - 「重置（应急换新）」→ 旧凭证**立即** RETIRED（无并存期）。
- 预期：遮显规则 = 只显示 `****` + 尾 4 位指纹，**永不回显明文**（含编辑回显）；两 kind 各自成链。
- 原理：凭证实时读（M5 明确不进链缓存）→ 轮换即时生效，无需任何缓存失效。

### C3 分组管理
- 目的：分组从属于应用、应用内名称唯一、删除守卫。
- 操作：分组管理 → 新建：所属应用选 `TESTAPP01`（或直接在各应用下的「分组详情」内联创建）→ 名称 `演示分组` → 排序 0。再建同名分组应被拒；该分组下建了接口后删分组应被拒（先删/移接口）。
- 预期：接口页「分组」下拉能按当前所选应用过滤出该分组；分组详情内可见组内接口并可点击跳转打开接口详情。

### C4 适配器管理（impl 目录驱动表单）
- 目的：三类适配器注册表、参数按 impl schema 动态渲染与校验、凭证类参数不落适配器配置、**D6 (impl,version) 维度**启用约束。
- 操作：适配器管理 → 顶部 tab 全部/鉴权/协议/报文 →「＋ 新建适配器」。关键填值示例（**填什么随 impl 走**，后端 `AdapterImplCatalog` 提供动态表单）：
  - **鉴权 - HMAC 签名**（HmacAuthAdapter）：标识 `ADP-T-HMAC`、名称随意、类型=鉴权、实现=`HMAC 签名`、签名算法选 `HMAC-SHA256`、签名 Header 名选 `X-Signature`、时间戳容差(秒)=`300`、防重放=关、版本 `1.0`、启用=开；
  - **报文 - 信封报文适配**（EnvelopeMessageAdapter）：实现=信封报文适配、业务数据容器 `data`、上游状态码字段 `code`、成功值 `0`、错误码映射可留空、消息字段 `message`、兜底错误码留空；
  - **凭证类字段**（ApiKey 的 apiKey 等）不会出现在参数表单——后端显式报「凭证类参数不落适配器配置，请在应用凭证中维护」，这是**特性**不是缺陷；
  - 同 impl 同 version 第二行启用应被拒（提示停用同版本其他适配器）；**同 impl 不同 version 可并存启用**（M5 D6 放宽，灰度前提）。
- 预期：协议类实例（JSON/XML 编解码）即使建了也不参与绑定（自动推导），接口高级区下拉只会出现 鉴权 / 报文 实例。

### C5 接口 CRUD 与类型互斥（最易填错，逐字段核对）
打开接口管理 →「＋ 新建」。**先选类型**（OUTBOUND=出站中转 / INBOUND=入站回调），随后表单互斥联动：
- 出站：必填**上游路径**（相对路径，如 `/anything`；填 `https://…` 或含 `..` 被拒）、禁止填回调地址、字段类型只许 RESP、绑定只许 MESSAGE+AUTH；
- 入站：必填**回调地址**（完整 URL 且非内网，SSRF 白名单校验）、禁止上游路径、只许 ACK 字段、绑定 MESSAGE+CALLBACK_AUTH、**必填出站侧（送达报文）参数**；
- 类型互斥字段在切换类型时自动清空（前端 `onTypeChange`）；已保存接口改类型会二次校验。
- 状态行操作：草稿/下线 →「发布」；已发布 →「下线」；重复发布/重复下线被拒。详情「测试接口」只对出站显示、「模拟回调」只对入站显示（按钮门控）。
- 通用字段：方法 POST/GET/PUT/DELETE；协议入/出 JSON|XML（勾「入站=出站」联动）；读超时(ms) 默认 3000；最大重试默认 4；透传开关（出站）= 映射为空整体转发。

### C6 乐观锁（并发防覆盖）
- 原理：更新走 `version + 1 WHERE version=?`，两处同时编辑后提交，后提交者 0 行 → 40001。
- 操作：同一接口开两个编辑页签 → 页签 A 保存（v+1）→ 页签 B 保存 → 报「配置已被他人修改，请刷新后重试」；刷新后 B 基于新版本重编辑可保存。
- 手工模拟：直接调 PUT 时 version 故意填旧值 → 同一报错（40001 信封）。

---

## 5. Flow A 出站链路端到端用例（M2/M3 链路 + M4 容错）

> 分册：《M2手动验收测试方案.md》（httpbin 成功/Bearer/死信/补偿/超时）、《M4手动验收测试方案.md》（熔断/对账/TTL/死信重放/QPS/IP）。以下给出**统一场景与填值基准** + 分册引用。

### F1 应用+接口「黄金用例」四分支（映射空=透传；Body 区出站侧选 json 按钮填 raw 报文）

在管理面新建（全部走前端；WireMock 作上游时应用服务地址填 `http://localhost:18080`）：

| 场景 | 应用 | 接口 code | 平台路径 | 上游路径 | 期望分支 |
|---|---|---|---|---|---|
| ① 成功直通 | `M4DEMO` | IF-O-OK | `/t/ok` | `/echo-ok`（m4 stub 200） | SUCCESS code=0 |
| ② 4xx 死信 | 同上 | IF-O-DEAD | `/t/dead` | 自注册 401 stub（§11.3 模板把 status 改 401）→ 上游路径填该 stub 路径 | DEAD_LETTER + 死信 |
| ③ 5xx 补偿 | 同上 | IF-O-COMP | `/t/comp` | `/breaker-supplier`（m4 stub 恒 500） | 短重试耗尽 → COMPENSATING → worker 重放 |
| ④ 超时 UNKNOWN | 同上 | IF-O-UNK | `/t/unk` | `/slow-supplier`（m4 stub 5s > 3s 读超时） | UNKNOWN 50401 |
| ⑤ 熔断 | 同上 | IF-O-BRK | `/t/brk` | `/breaker-supplier` | 连续失败→OPEN→50202 短路 |

> 注：② 与 ③⑤ 共用 M4DEMO（WireMock 上游）即可闭环，不必引入 httpbin——httpbin 的 `/status/401` 用法留在 M2 分册原场景（HTTPBIN 应用）。

**每分支必验三点：调用方信封、库状态机轨迹、call_log 留痕。**
- 调用（信封校验）：`curl -s -X POST http://localhost:8080/t/ok -H "Content-Type: application/json" -d '{"hello":"world"}'`
- 状态机 SQL（§11.2 SQL-2/SQL-3）：按 app_id 查 status/attempt/error_code/next_retry_at 序列；
- 日志对照：M2 分册给出 5xx 补偿全轨迹日志样例（attempt 1→5 递增、耗尽转死信），照抄核对。

### F2 熔断三态（M4 核心，详见 M4 分册阶段二①）
- 填值与前置：接口指向 `/breaker-supplier`（恒 500），**连发 ≥12 次**（窗口 10s/最小 10 次/失败率 50% 默认参数）触发 OPEN；
- 观察：前几次每次 ~3.5s（4 次短重试耗尽），之后变 <0.5s 秒回——**OPEN 短路**；库状态 50202/COMPENSATING 顺延（不 incrementAttempt，防熔断期空转消耗重试预算）；
- 恢复验证：WireMock `priority=1` 注册 200 stub（§11.3 模板）→ sleep 31s（OPEN 30s）→ 放行 2 次探测 → HALF_OPEN→CLOSED 恢复；期间原 COMPENSATING 行由补偿 worker 补做成功。
- 原理：闸门在 @Retryable Invoker 调用前，按「接口+供应商」粒度；计数口径=每请求一次（内部重试不逐次计数）。

### F3 UNKNOWN 对账 + TTL 降级（M4，详见 M4 分册阶段二②③）
- 造数：调超时接口（读超时 3s < /slow-supplier 5s）→ 得 UNKNOWN；
- **人工对账**：Monitor 页「对账（UNKNOWN）」行 → 实际到达则「置为已到达（SUCCESS）」，未到则「置为未到达（COMPENSATING）」→ 弹窗**操作人必填**（`admin`）、依据说明可选（审计用）→ 保存；查 `reconcile_audit` 表 source=MANUAL 落一行（operator/reason 入库）；
- **TTL 降级**：不人工处理，等 10 分钟（`unknown-ttl-minutes`）→ 自动按 COMPENSATING 续期 → 审计 source=TTL；
- 语义：对账双来源均写审计，可追溯是谁/系统何时裁定的。

### F4 死信重放（M4 阶段三①）
- 前置：仓库/验收遗留 PENDING 死信可直接演示（biz 指向本地 WireMock 则安全重放）；
- 操作：Monitor 死信区 → PENDING 行「重放」→ 该死信**立即置 HANDLED**（再次重放被拒：“死信已处理（HANDLED）”），关联运行记录置回 COMPENSATING（attempt 清零）→ ≤3s 后补偿 worker 按 in_payload 重走整链 → 上游已恢复则 SUCCESS、仍失败则再次耗尽产生新死信；
- 重放语义：按死信 ref_id 关联的运行记录 in_payload 重走整链（配置/凭证取最新），不是简单转发原始响应。

### F5 QPS / 日配额 / IP 名单（M4 阶段三，防护层语义）
- QPS：应用编辑把 QPS 限流填 `50` → `wrk -t4 -c200 -d10s`（或 200 并发 curl）→ 通过量≈50/s、其余 42901「触发限流」；**拒绝不落运行表**（SQL-1 验证）。
- 日配额：日调用量上限填 `10` → 调 11 次 → 第 11 次 42902；重启后清零（单机内存口径）。
- IP 名单：IP 黑名单填 `127.0.0.1` → 本地调用 40103 拒绝；白名单填其它 IP → 本地调用被拒（同理由）。
- 单实例内存口径边界：多实例各自计数、QPS 固定秒级窗口交界最坏 2×limit——属**已知语义**非缺陷。

### F6 报文语义深度（映射/信封/XML，自动化已覆盖，手动抽查 1~2 例）
- 信封剥壳：应用默认报文适配器绑 Envelope（data/code/successValue=0）→ 上游回 `{"data":{...},"code":"0"}` → 调用方收到 data 即业务内容、非 0 业务码透传且状态机仍 SUCCESS（C2 口径）；
- RESP 白名单：声明 RESP 字段后 data 只含声明字段（XML `ts` 未声明被滤）；清空声明=不过滤；
- XML：见 M3 分册阶段二（同名 item 合并数组、number 类型解析、ts 过滤差异演示）；
- 每改一处映射/绑定后**先点「测试接口」再 curl**（详见 §8 chainTrace 一节）——这是 M2 起养成的快速验证习惯。

### F7 动态映射 6 操作手动抽查（M-1，补齐 UI 层逐 op 覆盖）
> 自动化已有 MappingEngineTest 14 例全 op + null_strategy 矩阵；M0-02 附录有 24 例预期输出矩阵。此处只要求**经管理面建接口、逐 op 各建一条规则做端到端冒烟**（同一接口重复编辑会生成版本快照，正好顺带验证 §8 V1）。建议在 IF-M-OP 接口（上游 `/echo-ok` 回显，用 WireMock `/__admin/requests` 看实际出站 body 最直观）上逐轮执行：

| op | 规则填法（source / target / param） | 入站 → 预期出站（查 WireMock 收到的 body） | 语义原理 |
|---|---|---|---|
| rename | `state` → `order_state`（param 空） | `{"state":"PAID"}` → `{"order_state":"PAID"}` | 保形重命名 |
| typeCast | `qty` → `qty_num`，param=`INT` | `{"qty":"5"}` → `{"qty_num":5}`（数字） | 按类型注册表转换（TypeRegistry） |
| enumMap | `state` → `order_state`，param=`PAID→已支付` | `{"state":"PAID"}` → `{"order_state":"已支付"}` | 枚举映射（多对用逗号分隔：`1→0, 2→1`） |
| default | source 空 → `region`，param=`CN` | 任意入站 → 恒含 `{"region":"CN"}` | 常量注入（仅 default 可空 source） |
| condition | `state` → `state`，param=`state == 'PAID'` | PAID → 保留原值；其他值 → 按 null_strategy（KEEP=出站不含该字段） | Aviator 条件过滤（纯解释器防注入）；表达式内以入站字段名引用 |
| aggregate | `items`(数组) → `total`，param=`SUM` | `{"items":[1,2]}` → `{"total":3}` | source 须数组；SUM/MAX/MIN/CONCAT[:sep] |

> 入站字段含敏感键（token 等）时可在 Monitor 调用日志里顺带核对 body 已脱敏（§7）。

### F8 健壮性抽查（报文超限 / 长度越界 / SSRF）
- **报文 >1MB → 40002 且不落运行表**：双道防护（BodySizeLimitInterceptor 按 Content-Length 预检 + GatewayController 读后兜底）。
  ```bash
  head -c 1100000 /dev/zero | tr '\0' 'a' > /tmp/big.json
  curl -s -X POST http://localhost:8080/t/ok -H "Content-Type: application/json" --data-binary @/tmp/big.json
  # 预期：40002 报文超限信封；SQL-1 验证 outbound_request 无新行
  ```
- **变更说明/回滚字段越界 → 友好 400 而非 500**（M5 H2 修复回归）：PUT 带超 250 字 `X-Change-Note` → 40001「变更说明超长」；回滚 operator/reason 超 100 字 → 400 校验信息；
- **SSRF（回调地址）——按开关状态断言（重要）**：`application.yaml` 当前 `callback-allow-private: true`（**dev 默认放行本地回环**，本地 WireMock stub 验收的前提，勿改）→ 入站接口回调地址填 `http://127.0.0.1:18080/x` **保存通过**（属预期）；真实验证拒绝需把开关改 `false` 后重启后端（可选，自动化 CallbackUrlValidatorTest 4 例为主防线）；出站上游路径带 `http://` 或 `..` → 保存被拒（C5 已含，此处做负向复核一次即可）。

---

## 6. Flow B 入站回调链路端到端用例（M3 能力，分册含全填值表）

> 分册：《M3手动验收测试方案.md》阶段三（逐字段 4 张表：凭证/适配器/应用绑定/接口六段）。这里给最小闭环基线 + 关键语义。

**最小闭环（照 M3 分册配置一遍，应用沿用 `M3DEMO`，与 §3 隔离命名 / 清理 SQL 对齐）：** CALLBACK 凭证（值 `demo-secret`）→ HMAC 回调验签实例 ADP-HMAC-DEMO（HMAC-SHA256 / X-Partner-Signature / 容差 300）→ 应用编辑绑定为「回调验签适配器」→ 入站接口 IF-CB-01（回调地址 `http://localhost:18080/delivery-ok`，IN 参 3 条 / OUT 送达参 3 条，保形映射 3 条 rename，ACK 字段 returnCode number + returnMsg string，CALLBACK_AUTH 绑定留空=继承应用默认）→ 发布。

| 用例 | 动作 | 预期（含为什么） |
|---|---|---|
| B1 正常闭环 | 详情「模拟回调」→ 发送 | 页面显示供应商视角 ack `{"returnCode":0,"returnMsg":"success"}` + 送达状态 ACKED；查 inbound_delivery 新行 ACKED、payload=送达报文（映射后 order_state） |
| B2 验签失败 | curl 手发错签名 / 时间戳超容差 | HTTP 401（40100 签名错误 / 40101 过期）；**查库无新行**（验签失败不落运行表 D7）；call_log 有记录可查 |
| B3 ack 与送达解耦 | 回调地址改 `/delivery-flaky`（前 8 次 500 后放行）→ 模拟回调 | ack 仍即时回（供应商不重发）；库 PENDING attempt=1 → worker 每 3s 重送递增 → 放行后 ACKED——手动可见 PENDING→ACKED |
| B4 快照漂移防护 | 回调地址改 `/delivery-fail`（恒 500）→ 重送将耗尽前把接口回调地址改 `/delivery-ok` | 重送**仍按旧快照** `/delivery-fail` 失败（callback_url_snapshot 快照语义）→ 耗尽 DEAD_LETTER + dead_letter 落库（biz_type=INBOUND） |
| B5 送达 4xx | 回调地址指向返回 4xx 的 stub/地址 | 送达失败 → 死信（不无限重试 4xx） |
| B6 签名正确性交叉 | 模拟回调自签名成功 + 手工重放同一报文两次（防重放开时） | 开防重放 → 同签名在容差窗口内第二次被拒 40100「重复请求（防重放）」 |

**密钥外泄回归点（M3 评审缺陷 1 修复）：** 入站送达请求**不得带**应用出站签名头（WireMock 送达目标收到请求的 headers 不应含 `Authorization: Bearer …`）——每次回归都该瞄一眼 WireMock 收到的送达请求头。

---

## 7. 可观测与告警用例（M4，Monitor 五区块 + Dashboard + Prometheus）

| 区块 | 验证点（怎么验） |
|---|---|
| 统计卡 | 今日调用量/成功率随 F 系用例真实变化；对账/死信卡片数字与库一致；**今日成功率口径** = 今日终态（SUCCESS+DEAD_LETTER）中 SUCCESS 占比、当日无终态时显示 100（Dashboard 卡副标题同口径，核对数字勿按「全部请求」理解） |
| 调用日志 | 双向（IN 接入 / OUT 上游）都有记录；latencyMs 合理；**密钥字段脱敏**：header 值显示前 4 + \*\*\*\* + 后 4（值长 < 12 全遮 `\*\*\*\*`；如 `Bearer demo-token` → `Bear\*\*\*\*oken`，实测曾见 `Bear\*\*\*\*6789`），敏感键含 authorization/x-api-key/x-auth-token/x-signature/x-partner-signature/cookie 等（不区分大小写）；body 内 `secret/token/password/apikey/credential` 等敏感 JSON 键的字符串值同规则遮蔽、大陆手机号遮中段；超 4096 字符截断缀 `...[truncated]`。断言：**无明文**、traceId 与后端日志一致可全文检索 |
| 对账 UNKNOWN | 见 F3：按钮、审计落库、TTL 自动 |
| 死信 | 见 F4：状态/死因/reason、PENDING 才可重放 |
| 告警 | 规则管理「＋新建规则」：规则名 `演示告警`、指标选 `死信PENDING堆积（条）`、阈值表达式填 `> 0`、通知渠道留空（预留）、启用开 → 保存；扫到触发后 alert_event 出现记录、冷却 5 分钟；可删规则 |
| Dashboard | 今日统计 + 最近 8 条调用日志展示（M4 接真数据） |
| Prometheus | `curl -s localhost:8080/actuator/prometheus`：代码内 meter 点分隔，exposition 为下划线形态（Counter 带 `_total`）——`apicenter.calllog.dropped` → `apicenter_calllog_dropped_total`（**应恒 0/不增长**）、`apicenter.circuit.state` → `apicenter_circuit_state`、`apicenter.backlog` → `apicenter_backlog`、gateway 计数/延迟 → `apicenter_gateway_requests_total` / `apicenter_gateway_latency_*`、链与六阶段计时 `apicenter_chain_stage_seconds_*`；抽查 `curl -s localhost:8080/actuator/prometheus | grep -E "apicenter_(calllog|gateway|circuit|backlog|chain)"` |
| traceId 贯穿 | 一次失败请求：浏览器/curl 响应头 → call_log.trace_id → 后端日志同 traceId 行（TraceIdFilter MDC）→（可选 OTel span）链路收敛 |

---

## 8. M5 版本快照 / 灰度用例（详见 M5 分册，全库最新能力）

### V1 快照与回滚
- 前置：M5DEMO 应用指向本地 WireMock；出站接口 IF-M5-VER（平台路径 `/m5/ver`、上游 `/echo-ok`、映射 rename state→order_state）→ 发布；
- 操作与填值：**保存时底部「变更说明（可选）」**是 M5 新增输入：第一次留空（=v1）；编辑把映射 op 改「枚举映射」、param 填 `PAID→已支付`、变更说明填 `改枚举映射` → 保存（=v2）；
- 版本历史：详情 →「版本历史」→ 两条（v2 有说明 / v1 空）→ 点「快照」查看完整 config_json（可重建）；
- 回滚：v1 行 →「回滚到此版本」→ 弹窗**操作人必填** `admin`、依据 `演示回滚` → 确认 → 版本变 v3（**只增不倒退**）、状态仍「已发布」（快照不含 status）；
- 行为验证：curl `/m5/ver` 回滚后映射恢复 rename（上游收到的 body 是 `order_state=PAID` 而非「已支付」）；
- 异常：回滚到不存在版本 40403；并发（旧 currentVersion）40001；操作人留空前端拦截。
- 原理：回滚=用快照重建 InterfaceRequest 走**全量替换**路径（不旁路校验），乐观锁防覆盖；快照含 appId 不含 status。

### V2 灰度绑定与即时生效
- 前置（D6 放宽验证）：适配器管理新建两个**同实现**信封适配器实例并都启用：`ADP-ENV-1`（v9.1，codeField=`code`、successValue=`0`）与 `ADP-ENV-2`（v9.2，codeField=`status`、successValue=`ok`）——第二行启用不再报「同 impl 至多 1 条」；
- 绑定：出站接口高级 tab「报文适配器」选 `ADP-ENV-1`（第二个下拉 **版本**选 v9.1 或留空=跟随）→ 发布；上游（WireMock 改 stub，§11.3）回 A `{"data":{"v":1},"code":"0","status":"bad"}` → 调用方收到 v=1（按 code=0 判成功）；
- **切换灰度**：编辑把版本下拉改 v9.2 → 保存（变更说明可填「灰度切 v2」）→ 上游回 B `{"data":{"v":2},"code":"x","status":"ok"}` → **立即**（不等 5 分钟）curl 收 v=2——ConfigChangedEvent 事件失效即时生效的实测；
- chainTrace：详情「测试接口」响应 data 含 `chainTrace:[{role:MESSAGE,adapterId:ADP-ENV-2,impl:EnvelopeMessageAdapter,version:9.2}, …]`（test 端点强制实时解析，永远反映当前配置）；
- 容灾回退：版本填不存在的 `9.9` → 调用不 500，自动回退（应用默认→Noop 直通）且后端日志 warn「适配器绑定回退」；停用 ADP-ENV-1 → 同样回退；重新启用恢复；
- 留痕三通道对照：后端日志「链装配 … 绑定解析 → MESSAGE=…」行（通道 1）与 chainTrace（通道 3）内容一致；span tag（通道 2）仅压测/链路面看。

---

## 9. 性能与稳定性（M5.3，脚本随仓库 `src/test/resources/m5-load/`）

> 完整命令与造数 SQL 见 `m5-load/README.md`；结果须写入《M5压测报告.md》（环境/数据/结论/调优清单/同步送达决策）。

| 场景 | 目的 | 关键断言 |
|---|---|---|
| P1 出站吞吐（wrk 4t×100c×30s，POST JSON 黄金用例同构体） | 30s 吞吐/P99；对比 M4 基线（远程库下烘焙后 P99 应下降） | 错误率 <0.5%、无 5xx |
| P2 QPS 阈值 | 限流生效不雪崩 | 通过量≈limit、其余 42901、运行表无污染 |
| P3 补偿批量扫描（1 万条 COMPENSATING 造数 → 观察消化） | worker 批量吞吐与公平性 | 消化总时长、无重复重放（每行 attempt+1 恰一次） |
| P4 call_log 零丢弃 | 异步写不丢不阻塞 | P1 期间 `apicenter_calllog_dropped` 恒 0 |
| P5 同步送达（入站端点 wrk） | 送达+ack 时序结论（决策产物） | 见压测报告 |
| 7×24 冒烟（可选） | 长跑稳定性：OOM/连接池/慢查询 | 内存平稳、无堆栈溢出类日志 |

**原因与口径（写报告时引用）：** 读放大收益来自 M5.2 解析上移（绑定/映射/参数烘焙，凭证仍实时）；主链路每请求仍读 interface+app（状态/baseUrl 需实时）；远程 PolarDB ~百毫秒/SQL → **绝对值不作 KPI，只作相对基线**，贴近生产必须本地库复测。

### 9.5 本轮不覆盖与已知边界（评审补充，防误认为测试漏项）
- **已由自动化覆盖、手动不重复**：XXE 与 XML 深度上限（XmlProtocolAdapterTest）、动态映射全 op × null_strategy 矩阵（MappingEngineTest 14）、验签/网关/脱敏/告警/熔断纯单测（M4 各 XxxTest）；
- **需真实外部依赖、验收环境不具备**：真实云厂商签名（TC3/AWS4/ACS3/云回调验签）、真实 fastmoss token 联调（M2 分册阶段三标注可选）、证书链（AWS SNS 验签证书）；
- **等待成本高、改由自动化或专项覆盖**：UNKNOWN TTL 10 分钟自动降级（M4 分册标 10 分钟等待）、24h ROTATING 到期自动失效（演示并存 24h 只验证并存语义不验证到期瞬间）、7×24 长稳（可选）；另回调内网地址拒绝属**关闭开关态**验证（`callback-allow-private=false` 才生效，dev 默认 true 放行本地 stub），手动可选、以 CallbackUrlValidatorTest 为主；
- **架构级已知语义（非缺陷，断言勿踩）**：QPS/日配额/熔断/告警为**单实例内存口径**（多实例各自计数、重启清零，v1.1 分布式）；QPS 固定秒级窗口交界最坏 2×limit；死信重放与补偿依赖上游对 biz_id 幂等（ADR 5，无平台侧幂等开关）；回滚/版本历史不覆盖适配器定义自身（只恢复接口配置与绑定引用）。

---

## 10. 回归矩阵与执行计划

### 10.1 每轮（每次代码变更后）固定动作
1. `mvn clean test`（**改结构必须 clean**——旧 class 残留会被 Spring 扫描装配；集成测试自动起 WireMock:18080，手动环境停着）；
2. 归属核对：全库 174 = M1 22 / M2 23 / M3 62 / M4 57 / M5 10；
3. 冒烟三连（真链路冒烟）：F1 ①②分支 + B1 + V1/V2 各一条（都指向本地 stub）；
4. 观察类回归（改动涉及 M4/M5 时才全跑）：脱敏抽查、事件即时生效、乐观锁。

### 10.2 里程碑出口验收（正式验收顺序）
| 轮次 | 执行内容 | 对应方案 | 出口（全过才进下一轮） |
|---|---|---|---|
| R1 | M1 配置面全走查（§4） | M2 分册阶段一 | 五模块 CRUD + 状态机 + 凭证轮换 + 前端按钮门控 |
| R2 | Flow A 四分支 + 配置即生效 | M2 分册阶段二 | G1-G4 端到端（含日志/库轨迹） |
| R3 | XML + Flow B | M3 分册 | 阶段二 XML 语义 + 阶段三 B1-B6 + 验签失败不落库 |
| R4 | M4 可观测 + 容错 | M4 分册（35 分钟三阶段） | 熔断/对账/TTL/死信/告警/限流/脱敏/traceId/指标全过 |
| R5 | M5 版本与灰度 | M5 分册（30 分钟三阶段） | V1/V2 全过 + chainTrace/事件即时/回退 |
| R6 | 压测 + 总巡检 | m5-load + 第 9 节 | 压测报告产出 + call_log 零丢弃 + 全功能 walkthrough |

> 注：外网不可达时，R2/R4 的 4xx 分支与超时分支均可用 §5 F1 的本地等价实现（m4 stub + 自注册 401），httpbin 仅为 M2 分册原文场景。

### 10.3 缺陷管理约定
- 缺陷三要素必填：复现步骤（含填值/选项）、预期 vs 实际、影响面（影响哪条链路/语义）；
- 归属分类：语义缺陷（映射/信封/状态机口径）优先于 UI 缺陷；「已知语义（文档明示）」不算缺陷（如单机限流口径、日志口径、ack 解耦）；
- 修复后须回填：修复代码位置、补的自动化用例、本纲/分册是否需要校准。

### 10.4 验收收尾清理（跑完手动验收后必做，防污染后续轮次）
```sql
-- 清理手动造数（M2/M3/M4/M5DEMO、HTTPBIN、TESTAPP01 等，按需替换 app）
DELETE dl FROM dead_letter dl JOIN outbound_request o ON o.id=dl.ref_id AND dl.biz_type='OUTBOUND'
 WHERE o.app_id IN ('HTTPBIN','M4DEMO','M5DEMO');
DELETE dl FROM dead_letter dl JOIN inbound_delivery d ON d.id=dl.ref_id AND dl.biz_type='INBOUND'
 WHERE d.app_id IN ('M3DEMO','M4DEMO','M5DEMO');
DELETE FROM outbound_request  WHERE app_id IN ('HTTPBIN','M3DEMO','M4DEMO','M5DEMO');
DELETE FROM inbound_delivery  WHERE app_id IN ('M3DEMO','M4DEMO','M5DEMO');
DELETE FROM call_log         WHERE app_id IN ('HTTPBIN','M3DEMO','M4DEMO','M5DEMO');
-- reconcile_audit 无 app_id 列，按关联出站记录清
DELETE ra FROM reconcile_audit ra JOIN outbound_request o ON o.id=ra.outbound_request_id
 WHERE o.app_id IN ('HTTPBIN','M4DEMO','M5DEMO');
-- 应用/分组/接口/适配器/凭证：前端或按 cascade 删除（存在运行数据的接口只能先下线——顺序：先清运行表再删接口）
```
> 注意：seed（fastmoss / M4DEMO 种子用例）属预期保留数据，勿删；WireMock 手动实例验完关闭，避免占 18080 阻塞后续 `mvn test`。

---

## 11. 工具箱（附录）

### 11.1 枚举与值域总表（所有「选什么」的依据）
| 概念 | 取值 | 备注 |
|---|---|---|
| 应用状态 | DRAFT 草稿 / ENABLED 启用 / DISABLED 停用 / CANCELLED 注销 | 停用即拒请求 40102 |
| 接口状态 | DRAFT 草稿 / PUBLISHED 已发布 / OFFLINE 下线 | 发布/下线不生成版本快照 |
| 接口类型 | OUTBOUND 出站中转 / INBOUND 入站回调 | 互斥字段按类型清空 |
| HTTP 方法 | POST / GET / PUT / DELETE | |
| 协议 | JSON / XML | 协议适配器自动推导、不参与绑定 |
| 参数 type | string / number / boolean / object / array | 入站声明（XML 全文本解码提示） |
| 映射 op | rename / typeCast / enumMap / default / condition / aggregate | condition 表达式 Aviator 5，纯解释器防注入 |
| null_strategy | KEEP / NULL / DEFAULT / ERROR | |
| 绑定角色 | MESSAGE（报文）/ AUTH（出站签名，仅 OUTBOUND）/ CALLBACK_AUTH（回调验签，仅 INBOUND） | version 空=跟随当前实例，非空=灰度（同 impl 定位版本） |
| 凭证 kind | OUTBOUND 出站签名 / CALLBACK 回调验签 | 状态 ACTIVE/ROTATING/RETIRED；只显尾 4 指纹 |
| 适配器类型 | auth 鉴权 / protocol 协议 / message 报文 | 实现清单见 `AdapterImplCatalog`（下拉/参数表单据此渲染） |
| 防护错误码 | 42901 QPS / 42902 日配额 / 40103 IP 拒绝 / 40102 应用停用 | 防护层拒绝不落运行表 |
| 链路错误码 | 40002 报文超限（1MB）/ XML 深度超限 / 50201 重试耗尽 / 50202 熔断短路 / 50401 UNKNOWN / 40100·40101 验签失败 | 40002 为接入层与解码层共享 |
| 配置/回滚错误码 | 40001 参数/乐观锁冲突 / 40401 接口不存在 / 40403 快照不存在 | 40403 为 M5 新增 |

### 11.2 常用 SQL（Navicat 连 PolarDB，库 apicenter）
```sql
-- SQL-1 防护拒绝是否污染运行表（应无新行）
SELECT COUNT(*) FROM outbound_request  WHERE app_id='M4DEMO';
SELECT COUNT(*) FROM inbound_delivery  WHERE app_id='M4DEMO';

-- SQL-2 出站状态机轨迹（attempt 递增序列 / error_code）
SELECT id,status,attempt_count,max_attempts,error_code,next_retry_at,biz_id
FROM outbound_request WHERE app_id='M4DEMO' ORDER BY id DESC LIMIT 10;

-- SQL-3 死信
SELECT * FROM dead_letter ORDER BY id DESC LIMIT 10;

-- SQL-4 UNKNOWN 对账审计（双来源 MANUAL/TTL）
SELECT * FROM reconcile_audit ORDER BY id DESC LIMIT 10;

-- SQL-5 告警事件
SELECT * FROM alert_event ORDER BY id DESC LIMIT 10;

-- SQL-6 M5 版本快照核对（回滚后验证只增）
SELECT interface_id,version,change_note,created_at FROM interface_snapshot
WHERE interface_id=(SELECT id FROM interface WHERE code='IF-M5-VER') ORDER BY version DESC;

-- SQL-7 送达状态（B 系用例）
SELECT id,delivery_status,attempt_count,max_attempts,callback_url_snapshot,ack_to_partner
FROM inbound_delivery ORDER BY id DESC LIMIT 5;
```

### 11.3 常用 curl
```bash
# 调用平台接口（出站）
curl -s -X POST http://localhost:8080/<平台路径> -H "Content-Type: application/json" -d '<入站报文>'

# 携带 traceId 以便日志关联
curl -s ... -H "X-Trace-Id: manual-$(date +%s)"

# WireMock 换 stub：先清原 mapping 再注册（priority=1 覆盖同名；改 status 即造 4xx/5xx 分支）
curl -s -X POST http://localhost:18080/__admin/mappings -H "Content-Type: application/json" -d '{
  "priority":1,"request":{"method":"POST","url":"/status-401"},
  "response":{"status":401,"body":"denied"}}'
curl -s -X POST http://localhost:18080/__admin/mappings -H "Content-Type: application/json" -d '{
  "priority":1,"request":{"method":"POST","url":"/breaker-supplier"},
  "response":{"status":200,"jsonBody":{"data":{"v":2},"code":"x","status":"ok"}}}'
curl -s -X POST http://localhost:18080/__admin/scenarios/reset     # 重置 scenario 状态
curl -s http://localhost:18080/__admin/requests?limit=5 | jq       # 查看 WireMock 收到的请求（验证出站 body）

# Prometheus 指标抽查
curl -s localhost:8080/actuator/prometheus | grep -E "apicenter_calllog_dropped|apicenter_chain"
```

### 11.4 文档导航（测试相关）
- 分册：`doc/开发文档/M2|M3|M4|M5手动验收测试方案.md`（执行细节）；`M4开发计划.md` §熔断参数口径、`M5开发计划.md` D-M5-1~3（验收对照上游）；
- 设计事实来源：`doc/API中心设计方案.md`（§5.5 ack 解耦 / §6 状态机容错 / §3.1 类型互斥）；`doc/API中心时序图与流程图.md`（Flow A/B 时序）；
- 踩坑：`doc/开发文档/技术踩坑记录.md`（Spring 7/WireMock 3/M5 各坑——写用例脚本前先查）；
- 版本：`doc/开发文档/M5手动验收测试方案.md`（含本纲 V 系的勾选式预期，M5 收口以它为准）。

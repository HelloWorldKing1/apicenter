# 应用凭证配置改造设计方案（应用弹窗内联凭证）

> 版本：v0.2（2026-09-11 评审通过并落地）｜范围：管理面「应用管理」新增 / 修改弹窗内联凭证录入（前端为主 + 后端两项小修）
> **拍板结果（2026-09-11）**：D1 = 方案 A（凭证仍按 `(app, kind)` 存）；D2 = 前端串行 + 失败补救；D3 = 留空即不改动；D4 = 多 secret 字段 JSON 化 + 未交付实现可填但提示；D4b = 角色推断（E4 元数据延后）；D5 = 凭证缺失不阻断保存（软提示）。**E1 + E2 已落地**，E3/E4 延后；`API中心原型.html` 本期不动。
> **实现状态**：已落地（见 §13 落地记录）；后端 M1 集成测试 16/16 通过（含新增 2 条 E1/E2 用例），前端 `vite build` 通过。
> **目标读者**：评审选型后即编码。文中所有约束均对照现有代码（文件 + 行号）给出：凭证状态机与 M0-04 一致，**不新增旁路写入**，不改 `app_credential` 表结构（方案 A）。
> **一句话**：把凭证入口从「应用详情抽屉 + 手工选 kind」搬到「应用弹窗内、紧跟签名适配器、按适配器 impl 渲染该适配器需要的凭证字段」，落库仍走既有 `POST /apps/{appId}/credentials/update|reset`。

---

## 1. 背景与目标

### 1.1 现状（代码事实）

| # | 事实 | 位置 |
|---|---|---|
| 1 | 应用弹窗只有 3 个适配器下拉（供应商签名 / 回调验签 / 默认报文），**无凭证入口** | `frontend/src/views/Apps.vue` L62 / L70 / L78 |
| 2 | 凭证只能从「应用详情抽屉」维护，且必须**手工选凭证类型 kind**（OUTBOUND / CALLBACK），与当前选中的适配器无关 | `Apps.vue` L108–123（凭证弹窗）、L349–372（`openCredDialog/saveCred`） |
| 3 | 适配器 impl 元数据**已声明**凭证类字段：`ApiKeyAuthAdapter→apiKey`、`BearerTokenAuthAdapter→token`、`CloudSignatureAdapter→secretId+secretKey`、`CloudCallbackSignatureAdapter→token` | `service/AdapterImplCatalog.java` |
| 4 | 这些字段在适配器参数表单里被渲染成 **disabled 灰字**，提示「凭证值请到『应用管理 → 点击应用 → 凭证』中维护」 | `components/AdapterParamsEditor.vue` L36–37 |
| 5 | 后端**显式拦截** secret 落 `adapter.params`（400：「凭证类参数不落适配器配置」） | `service/AdapterService.java` L133 |
| 6 | `app_credential` 表注释已预留「复合凭证 JSON 化」`{"secretId":..,"secretKey":..}` | `doc/schema.sql` L261、`M0-04` §3.4 |
| 7 | 出站凭证注入 = 每请求实时读 `(app, OUTBOUND)` 的 ACTIVE，**不区分适配器**；凭证不进链缓存 | `engine/ChainEngine.java` L254 / L402–407、`config/ConfigChangedEvent.java` 注释 |
| 8 | 原型 `API中心原型.html` 本来就画的是「应用弹窗内 password 型 secret 字段」（单 secret / 应用） | `API中心原型.html` L818、L1141 |

### 1.2 痛点

- **认知断层**：选了「Bearer Token」适配器，界面不告诉你「它需要 Token」；用户按直觉找凭证，实际要「先保存应用 → 回列表点行 → 抽屉里选 kind → 填值」，跨 3 个界面。
- **错配风险**：kind 是手选的，回调验签密钥填到 OUTBOUND 不会报错，直到运行期验签失败（40100）才暴露。
- **静默降级**：未配置凭证时适配器只 `ctx.warn`，请求仍发出（缺签名头）→ 供应商 401，排查成本高（`ChainEngine` / `ApiKeyAuthAdapter`）。
- **新建应用首配缺失**：新建流程里完全没有凭证环节，必须二次进入。

### 1.3 目标与非目标

**目标**
1. 应用弹窗内，选择签名 / 回调验签适配器后，**就地出现**该适配器所需的凭证录入区；
2. 凭证录入区**跟随 impl 元数据**（0 字段不显示、1 字段单输入、≥2 字段多输入 → JSON 复合）；
3. 与 M0-04 状态机**完全兼容**：更新（旧值并存 24h）/ 重置（旧值立即失效）语义不变，指纹遮显、明文永不回显；
4. 新建应用可一次性配好凭证（保存应用 + 写凭证两步串行，失败可补救）。

**非目标（本期不做）**
- 不做「每个适配器实例一套独立凭证」（方案 B，见 §3/§7 D1）；
- 不做弹窗内的「平台生成随机值 → prepare → 激活」完整轮换流（保留在应用详情抽屉，弹窗内只给 update / reset + 状态提示）；
- 不改 `app_credential` 表结构、不改链引擎读取口径、不改 `AdapterService` 的 secret 拦截规则。

---

## 2. 关键约束（任何方案都必须满足）

| # | 约束 | 来源 / 事实 |
|---|---|---|
| C1 | 凭证按 `(app_id, kind)` 存储，kind ∈ {OUTBOUND, CALLBACK}，每 kind 至多一条 ACTIVE | `schema.sql` L257–268；`CredentialService.validateKind` |
| C2 | 管理面**永不回显明文**，仅尾 4 位指纹；`prepare` 生成的明文仅一次性回显 | `CredentialDtos` 注释、`CredentialService.listViews` |
| C3 | 更新 = 新值 ACTIVE、旧 ACTIVE → ROTATING（+24h 并存，仅回调验签参与并存）；重置 = 旧值全部立即 RETIRED | `CredentialService.update/reset`、`M0-04` §3.3 |
| C4 | 出站签名仅用 ACTIVE；回调验签用 ACTIVE + 未过期 ROTATING 逐个试 | `CredentialRepository.findActive/findVerifiable` |
| C5 | 凭证值**不落 `adapter.params`**（后端 400 拦截）；两处存密钥属明确禁止项 | `AdapterService.java` L133 |
| C6 | 凭证每请求实时读、不进链缓存 → 凭证变更**不需要**发 `ConfigChangedEvent` | `ChainEngine.injectCredential` L402；`ConfigChangedEvent` 注释（M5 二轮核实） |
| C7 | 凭证 API 依赖应用存在（`requireApp`），**新建弹窗内 appId 尚未落库** | `CredentialService.requireApp` |
| C8 | 接口版本快照 / 回滚（M5）**不覆盖凭证**：回滚接口版本不会回滚密钥 | `M5开发计划.md` D-M5-1（config_json 仅接口配置） |
| C9 | 接口级 AUTH 绑定可覆盖应用默认适配器，但**凭证仍按应用维度共享**（方案 A 下的已知语义） | `interface_adapter_binding.role=AUTH`；`ChainEngine.injectCredential` |

---

## 3. 候选方案

### 方案 A：UI 按 impl 渲染凭证字段，存储仍按 kind（**推荐**）

- **交互**：应用弹窗内，选中适配器 → 依据该 adapter 的 `impl` 从 `/adapters/impls` 取 `kind==='secret'` 的字段 → 渲染凭证卡片（label 用元数据 `label`，并展示现有 ACTIVE / ROTATING 状态与指纹）。
- **提交**：复用 `POST /apps/{appId}/credentials/update`（默认）或 `/reset`（用户显式切换）。多 secret 字段 → `JSON.stringify({...})` 作为 `credential` 值（与 `schema.sql` L261 注释、`M0-04` §3.4 一致）。
- **优点**：后端必要改动为 0；不动状态机 / 引擎 / 测试基线；用户心智与适配器一致；一次性解决 1.2 全部痛点。
- **缺点**：一个应用仍只有一条 OUTBOUND 凭证 —— 若同应用下不同接口绑定不同签名适配器且**各自密钥不同**，A 方案无法表达（→ 方案 B）。该限制需在界面与文档中明示（C9）。

### 方案 B：凭证按适配器实例归属 `(app_id, adapter_id, kind)`

- **语义**：`app_credential` 增 `adapter_id VARCHAR(16) NULL`（NULL = 应用级兜底）；键必须是 **(app_id, adapter_id)**，因为 `adapter` 是全局注册表（可被多个应用引用），只按 adapter_id 存会导致跨应用共用密钥。
- **引擎**：`injectCredential` 需拿到链上**实际生效的 auth 实例 id**（含接口级覆盖），回退序 = 接口覆盖实例凭证 → 应用默认实例凭证 → kind 兜底；每请求多一次索引查询。
- **优点**：同应用多签名体系（如 Bearer + HMAC 各一套密钥）语义正确；接口级覆盖不再有凭证歧义。
- **代价**：DDL（列 + 索引 + M5 生产加固的唯一约束需改成含 adapter_id）+ 全套凭证 API 加 scope 参数 + 前端凭证区从「跟 impl」变「跟实例（一个应用可能 N 个签名实例）」+ M1/M2/M3 集成测试与 seed / 演示脚本回归；≈ 3–5 人日。
- **建议**：本期不落地，作为 D1 的备选；触发条件 = 出现真实「同应用多签名密钥」需求。

### 方案 C：新建应用时后端原子端点（`POST /apps` 携带 credential 草稿）

- **语义**：`AppRequest` 增可选 `credentials[]`（kind + 明文），服务端在一个事务内「插 app → 写凭证」，避免前端串行两步的半成功状态。
- **优点**：原子、无「应用已建凭证失败」中间态；一次请求。
- **代价**：改 `AppDtos` / `AppService`（新增事务步骤与校验）、`AppRequest` 被凭证明文污染（版本快照 / 日志脱敏面扩大）、与「更新 / 重置」语义要在请求体里二选一表达；对「更新应用」路径语义含糊（是 update 还是 reset？）。
- **建议**：本期不做（D2 推荐前端串行 + 失败补救），若后续要求原子性再加。

### 方案对比

| 维度 | A（推荐） | B | C |
|---|---|---|---|
| 后端改动 | ≈0（+2 项建议小修） | 表 + API + 引擎 + 测试 | DTO + 事务 + 校验 |
| 语义正确性（多签名体系） | 部分（应用级共享） | ✅ 完整 | 同 A |
| 新建应用一步到位 | ✅（前端串行） | ✅ | ✅ 原子 |
| 回归面 | 前端 + 文档 | 全量（含 180 用例中的凭证相关） | 应用模块 + 脱敏 |
| 工作量 | ≈3 人日 | ≈3–5 人日（纯后端增量，不含前端返工） | ≈1.5 人日 |

---

## 4. 详细设计（方案 A）

### 4.1 组件结构与数据流

```
Apps.vue（新建 / 编辑弹窗）
 ├─ 供应商签名适配器  el-select ─┐ 选中 adapterId
 │                              └→ impl = allAdapters.find(id).impl
 ├─ <CredentialEntry :fields="credFields(form.authAdapterId)"
 │                  :credential="credState.OUTBOUND"     ← 编辑态 GET /apps/{appId}.credentials
 │                  :pending="!dialog.isEdit"
 │                  :editable="credentialNeeded(…)"
 │                  :warning="credWarning('OUTBOUND')"
 │                  v-model="credDraft.OUTBOUND"
 │                  v-model:mode="credMode.OUTBOUND" />
 ├─ 回调验签适配器   el-select ─→ <CredentialEntry … kind='CALLBACK'>
 └─ 默认报文适配器（无凭证字段，不渲染）
```

**新增单文件组件** `frontend/src/components/CredentialEntry.vue`（**纯展示**：不改造 `AdapterParamsEditor`，避免污染「适配器管理」页的只读语义）：

```js
// 字段清单与载荷组装都在 Apps.vue（父组件决定「需要什么、提交什么」），本组件只管渲染与双绑定。
props: {
  fields: Array,          // [{ key, label, required }]；父组件已含兜底（未声明 secret 字段时为 GENERIC_SECRET_FIELD）
  credential: Object,     // CredentialView | null（编辑态从详情取）
  pending: Boolean,       // 新建态：应用尚未落库
  editable: Boolean,      // false = 适配器不需要凭证，仅展示状态行
  warning: String,        // 缺失提示（D5 软提示）
  modelValue: Object,     // { [fieldKey]: 明文 }（仅内存，关闭弹窗即清）
  mode: String            // 'update' | 'reset'
}
emits: ['update:modelValue', 'update:mode']
```

`impl` 解析优先级：`form.authAdapterId` → `allAdapters.find(a => a.id === …)?.impl`（含停用实例，仅解析用）；若刚通过「＋ 自定义」内联创建，`load()` 后新实例已在下拉中，同样可解析（`saveInlineAdapter` 已回填 `form[targetField]`，`Apps.vue` L245–260）。

> 「显示哪张卡片」看角色 + impl（`credentialNeeded`），「卡片里画几个输入框」看 impl 的 secret 字段（`credFields`，缺失时退化为 `GENERIC_SECRET_FIELD`）——见 §4.4。

### 4.2 交互稿（编辑态）

```
┌ 编辑应用 ─────────────────────────────────────────────────────────┐
│ 应用标识   fastmoss（只读）                                        │
│ 应用名称   [FastMoss                          ]                   │
│ 服务地址   [https://openapi.fastmoss.com      ]                   │
│ 供应商签名适配器 [ Bearer Token·生产（BearerTokenAuthAdapter）▾ ][＋自定义] │
│ ┏ 出站签名凭证（OUTBOUND）──────────────────────────────────┐    │
│ ┃ ● ACTIVE  ****a1b2 · 2026-09-07 10:12 生效               │    │
│ ┃ Token *  [•••••••••••••••••••••••••••••••]  👁           │    │
│ ┃          该适配器（BearerTokenAuthAdapter）需要 Token      │    │
│ ┃ 写入方式  ◉ 更新（旧值并存 24h，覆盖供应商切流窗口）        │    │
│ ┃           ○ 重置（旧值立即失效，应急）                     │    │
│ ┃ ⓘ 保存后管理面仅显示尾 4 位指纹，明文永不回显               │    │
│ ┗─────────────────────────────────────────────────────────┘    │
│ 回调验签适配器 [ HMAC 回调验签（HmacCallbackVerifyAdapter）▾ ][＋自定义] │
│ ┏ 回调验签凭证（CALLBACK）─────────────────────────────────┐    │
│ ┃ ⚠ 未配置 —— 入站回调验签将直接 40100                      │    │
│ ┃ 密钥 *  [•••••••••••••••••••••••••••••••]                │    │
│ ┗─────────────────────────────────────────────────────────┘    │
│ 默认报文适配器 [ 直通（Noop）▾ ][＋自定义]                          │
│ QPS 限流 / 日调用量上限 / IP 白名单 / IP 黑名单 / 描述 …             │
└──────────────────────────────────────────────────────────────────┘
```

视觉规格：
- 凭证卡片**紧跟对应适配器**（不是页面底部），浅底（`#fafbfc`）+ 左侧 2px 主色条 + 8px 圆角，形成「适配器的从属块」语义；
- 状态行三态：`⚠ 未配置`（warning） / `● ACTIVE ****xxxx · 生效时间`（success） / `◐ 存在轮换中凭证（并存至 MM-DD HH:mm）`（warning）；
- 多 secret 字段（≥2）：字段纵向排列，全部同一张卡片；
- 高级轮换（`prepare` / 激活 / 完成轮换 / 吊销 / 历史）保留在详情抽屉，卡片底部给一行链接文案「高级轮换（生成 / 激活 / 收尾）在应用详情 → 凭证区」。

### 4.3 交互稿（新建态：草稿凭证 + 串行提交）

`CredentialService.requireApp()` 使「新建弹窗中 appId 未落库」时无法写凭证（约束 C7），故采用**草稿凭证 + 保存后串行提交**：

```
┌ 新建应用 ───────────────────────────────────────────┐
│ 应用标识 [QD1        ]   ← 未落库                    │
│ 供应商签名适配器 [ Bearer Token ▾ ][＋自定义]          │
│ ┏ 出站签名凭证（OUTBOUND）  〔待创建〕───────────────┐ │
│ ┃ Token *  [•••••••••••••••••••••••]               │ │
│ ┃ ⓘ 保存应用后自动写入（同一操作内串行两步）          │ │
│ ┗─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 4.4 渲染规则（显示条件 + 字段来源，唯一需要写进规范的两条）

**规则 1：卡片是否显示 —— 由「角色 + impl」决定，不由 secret 字段决定。**

因为 impl 元数据的 secret 声明**不完整**：`HmacCallbackVerifyAdapter`（回调验签主力实现）的 schema 里**没有** secret 字段，但运行时必须读 `kind=CALLBACK` 凭证（`HmacCallbackVerifyAdapter.verify()` L70 起，L108 `findVerifiable(appId, "CALLBACK")`）；`HmacAuthAdapter` 同样无 secret 字段且**无 Bean 实现**（`adapter/auth/` 仅有 `ApiKeyAuthAdapter` / `BearerTokenAuthAdapter` / `NoopAuthAdapter` / `HmacCallbackVerifyAdapter`）。

| 凭证卡片 | 显示条件 |
|---|---|
| 出站签名凭证（OUTBOUND） | 供应商签名适配器下拉已选中，且 impl ≠ `NoopAuthAdapter`；**或**该应用已有 OUTBOUND 凭证（编辑态，显示只读状态行） |
| 回调验签凭证（CALLBACK） | 回调验签适配器下拉已选中，且 impl ≠ 无鉴权；**或**该应用已有 CALLBACK 凭证 |

**规则 2：卡片内字段 —— 由 impl 的 secret 字段决定，缺失时退化为单通用输入。**

| secret 字段数 | 渲染 | 提交载荷 | 示例 impl |
|---|---|---|---|
| 1 | 单输入框（`type=password` + 眼睛开关），label 取元数据 `label` | 原样字符串 | `ApiKeyAuthAdapter.apiKey`、`BearerTokenAuthAdapter.token`、`CloudCallbackSignatureAdapter.token` |
| ≥2 | 同源多输入框 | `JSON.stringify({ [key]: value, … })`，**键名取元数据 key** | `CloudSignatureAdapter.secretId + secretKey` |
| 0（但规则 1 判定需显示） | 单通用输入框，label =「密钥 / Token」，附注「该适配器未声明凭证字段名，按单值密钥处理」 | 原样字符串 | `HmacCallbackVerifyAdapter`、`HmacAuthAdapter` |

补充规则：
- **留空 = 不改动**（凭证不可回显，空值不能解释为「清空」）；
- **部分填写 = 前端拦截**并列出缺失字段 label（避免写入半截复合凭证）；
- 复合凭证（≥2 字段）的**运行时消费方尚未交付**（`CloudSignatureAdapter` / `CloudCallbackSignatureAdapter` 无 Bean），凭证区对无 Bean 的 impl 显示 `〔实现未交付，凭证按 JSON 预留〕` 灰字提示，不阻断保存（D4）；
- 可选精化 E4（§5）：`ImplMeta` 增 `credentialKind`（`OUTBOUND` / `CALLBACK` / `null`），由后端元数据显式声明，替代规则 1 的角色推断——本期不做也能工作。

### 4.5 状态矩阵（评审逐行确认）

| 场景 | 凭证区表现 | 保存行为 |
|---|---|---|
| 未选签名适配器 / 该角色选了 Noop（无鉴权） | 不渲染 | 无 |
| 选中 impl 无 secret 字段（如 `HmacCallbackVerifyAdapter`） | 单通用输入框（§4.4 规则 2） | 同 ACTIVE 路径（原样字符串） |
| 新建 + 已填凭证 | `〔待创建〕` tag + 输入框 | 串行 `POST /credentials/update` |
| 编辑 + 未配置 | `⚠ 未配置` + 输入框（+ 若为 CALLBACK 且应用下有已发布入站接口 → 黄色硬警告） | 有输入才提交 |
| 编辑 + ACTIVE | 指纹 / 生效时间 + 输入框 | 默认 `update`（旧值 ROTATING 24h） |
| 编辑 + 用户切「重置」 | 红字风险说明 + `ElMessageBox.confirm` | `POST /credentials/reset` |
| 编辑 + 存在未过期 ROTATING | 黄条「存在轮换中凭证（并存至 …），建议先激活或收尾」 | 仍可 update；提示 `prepare` 会被 400（缺陷 D-1） |
| 编辑 + ROTATING 已过期 | 灰条「已过期（惰性视为失效）」 | 同 ACTIVE 路径 |
| impl 无 secret 字段但存在历史凭证 | 一行只读状态 + 「去详情管理」 | 不动凭证 |

### 4.6 提交时序（`Apps.vue save()` 改造）

```js
async function save() {
  const payload = { ...form }                 // 显式构造：只含 AppRequest 字段（form 无凭证键）
  const drafts = collectCredentialDrafts()    // [{ kind, credential, mode }]，未填 → payload=null 不入队
  if (!drafts.ok) { ElMessage.warning(drafts.message); return }   // 部分填写拦截
  if (drafts.items.some(i => i.mode === 'reset')) await confirmReset()  // 重置不可逆 → 二次确认

  if (dialog.isEdit) {
    await http.put(`/apps/${form.appId}`, payload)
    await submitCredentials(form.appId, drafts.items)     // 失败抛错（凭证端报错原样暴露），不回滚应用
    ElMessage.success(drafts.items.length ? '应用与凭证已保存' : '保存成功')
  } else {
    await http.post('/apps', payload)                     // 1) 建应用（DRAFT，§C7）
    try {
      await submitCredentials(form.appId, drafts.items)   // 2) 写凭证（update：无旧值 → 直接 ACTIVE）
      ElMessage.success(...)
    } catch (e) {
      ElMessage.warning('应用已创建，但凭证写入失败，请重新进入编辑补填')
      dialog.visible = false; await load(); await openEdit(apps.value.find(a => a.appId === form.appId))
      return
    }
  }
  dialog.visible = false; load()
}

// 逐个串行提交（便于定位是哪个 kind 失败）；action 由 mode 决定：update | reset
async function submitCredentials(appId, items) {
  for (const it of items) {
    const action = it.mode === 'reset' ? 'reset' : 'update'
    await http.post(`/apps/${appId}/credentials/${action}`, { kind: it.kind, credential: it.credential })
  }
}

// 载荷映射（§4.4 规则 2）：单字段 → 原样字符串；多字段 → JSON（键名取元数据 key）
function buildCredential(kind) {
  const fields = credFields(adapterIdOf(kind))
  const filled = fields.filter(f => String(credDraft[kind]?.[f.key] ?? '').trim() !== '')
  if (filled.length === 0) return { ok: true, payload: null }              // 留空 = 不改动（D3）
  if (filled.length < fields.length) return { ok: false, message: '请填写完整的 …' }
  const pairs = fields.map(f => [f.key, String(credDraft[kind][f.key]).trim()])
  return { ok: true, payload: pairs.length === 1 ? pairs[0][1] : JSON.stringify(Object.fromEntries(pairs)) }
}
```

要点：
- **必须显式构造 payload**（`{ ...form }`；form 只含 AppRequest 字段），保证凭证明文不长在 `/apps` 请求体里；
- 编辑态打开弹窗时额外 `GET /apps/{appId}` 取 `credentials[]` 填充状态行（现 `openEdit` 只用列表行数据，`Apps.vue` L281–292）；列表接口的 `credentials` 恒为空（列表不带子表），**不要**指望列表行；角标改用 E1 的 `has*Credential` 布尔；
- 编辑态**顺带** `GET /interfaces?appId=…` 判断是否存在已发布入站接口（CALLBACK 强告警依据），失败静默降级为不提示；
- 内联创建适配器（`＋ 自定义`）后 `load()` 刷新下拉 → impl 解析自动生效；`AdapterParamsEditor` 内的 secret 字段保持 disabled（它属于适配器管理语义），凭证输入只出现在应用弹窗的凭证卡片里。

### 4.7 文案与安全

- **明文永不回显**：输入框 `type=password`；眼睛开关仅本弹窗内有效，关闭弹窗即清空草稿；不写 `sessionStorage` / `localStorage`；
- 只做 `trim` + 非空校验，不限长度/字符集（供应商密钥形态各异，如 JWT 带点号）；
- 只有 `reset` 走二次确认（不可逆语义）；`update` 不弹窗；
- 卡片底部固定一行说明：`保存后仅显示尾 4 位指纹；接口版本回滚不会回滚凭证（凭证独立于接口快照）`（对应约束 C8）。

### 4.8 校验与提示（不阻断保存，D5 推荐）

| 情况 | 级别 | 文案 |
|---|---|---|
| 选中非 Noop 适配器但该 kind 无 ACTIVE 凭证 | 黄色 alert（可保存） | 「未配置出站签名凭证，运行期请求将不带签名头（供应商 401）」 |
| 应用下存在已发布 INBOUND 接口 + CALLBACK 无凭证 | 黄色 alert（可保存） | 「入站回调验签将直接 40100，请补充回调验签凭证」 |
| 用户切「重置」 | 红色确认框 | 「旧凭证立即失效且不可恢复，确认重置？」 |
| 复合凭证部分填写 | 阻断 + 字段级错误 | 「请填写完整的 <label1> / <label2>」 |

---

## 5. 后端改动（方案 A）

**必要改动：0 行**（复用 `POST /apps/{appId}/credentials/update|reset`、`GET /apps/{appId}`）。

建议一并落地的 4 项小修（E4 可选，均 ≤0.5 人日）：

| 编号 | 内容 | 位置 | 理由 |
|---|---|---|---|
| E1 | 列表响应增加 `hasOutboundCredential` / `hasCallbackCredential` 布尔（一次 `GROUP BY kind` 查询，非 N+1） | `AppDtos.AppResponse` + `AppRepository` | 应用列表可加「凭证缺失」角标，巡检入口 |
| E2 | `prepare` 的 ROTATING 计数排除已过期行：`AND (rotating_until IS NULL OR rotating_until > NOW())` | `CredentialRepository.countByStatus` 或新增专用方法 | 修既有缺陷 D-1（§10） |
| E3 | `ChainEngine.injectCredential`：若明文是 JSON 对象，额外塞 `outboundCredentialFields` Map（同时保留原字符串） | `ChainEngine` L402 | 为复合凭证的运行时消费方（云厂商签名）预留通道，A 方案可不做 |
| E4（可选） | `ImplMeta` 增 `credentialKind` 字段并在 `AdapterImplCatalog` 显式标注（`ApiKey/Bearer/CloudSignature→OUTBOUND`；`HmacCallbackVerify/CloudCallbackSignature→CALLBACK`；`Noop→null`） | `AdapterDtos.ImplMeta` + `AdapterImplCatalog` | 用元数据替代前端「角色推断」（§4.4 规则 1），消除 `HmacCallbackVerifyAdapter` 无 secret 字段导致的推断歧义 |

---

## 6. 影响面清单

| 面 | 影响 | 等级 |
|---|---|---|
| 表结构 | **无变更**（复合凭证 JSON 已被 `schema.sql` L261 / `M0-04` §3.4 认可） | 🟢 |
| 后端 API | 无破坏性变更；E1 为新增字段（向后兼容） | 🟢 |
| 链引擎 / 状态机 | 无变更（凭证每请求实时读，C6 → 不需要 `ConfigChangedEvent`） | 🟢 |
| 前端 | `Apps.vue` 弹窗改造（表单结构 / 数据获取 / save 时序）+ 新增 `CredentialEntry.vue`；`AdapterParamsEditor` 灰字提示文案微调（L37）；`Adapters.vue` 保持只读不变 | 🟡 |
| 原型 | `API中心原型.html` 全文 grep「凭证」0 命中（原型本就用 password 型 secret 字段），**可不动**；若坚持「原型即事实来源」，补一个凭证卡片示意 | 🟢 |
| 自动化测试 | 后端不受影响（180 用例口径不变，凭证用例走 API 层）；前端无测试框架 → 仅手动用例 | 🟢 |
| 文档 | 需同步 6 处：`API中心使用教程.md` §4.1/4.2（凭证入口路径改为「应用弹窗 → 凭证卡片」）、`端到端闭环演示方案.md` §2（A1/A2 合并为「应用（含凭证）」）、`整体测试方案.md` C2/C4、`M2手动验收测试方案.md` 阶段一 + ②、`M3手动验收测试方案.md` 阶段一、`M5手动验收测试方案.md` 阶段三步骤 3 全功能 walkthrough | 🟡 |
| 演示脚本 | 顺序由「应用 → 凭证 → 适配器」变为「适配器 → 应用（含凭证）」；讲稿 1 段改写 | 🟡 |
| 运维语义（**需写进文档**） | ① 接口版本回滚不回滚凭证（C8）；② 接口级 AUTH 覆盖只改「用法」不改「凭证」（C9）；③ 同一应用所有出站接口共享一条 OUTBOUND 凭证（方案 A 边界） | 🔴 |

---

## 7. 关键语义决策（评审重点）

| 编号 | 决策 | 选项 | 推荐 | 理由 |
|---|---|---|---|---|
| **D1** | 凭证归属粒度 | A：`(app, kind)` 不变，UI 按 impl 渲染 / B：`(app, adapter_id, kind)` | **A 先落地** | B 的真实触发条件是「同应用多签名密钥」，当前无实例；A 后端零改动、回归面最小 |
| **D2** | 新建应用时凭证时序 | ① 前端串行两步（失败可补救）/ ② 后端原子端点（方案 C）/ ③ 新建置灰，保存后再配 | **①** | 不新增 API 面；失败路径明确且用户可自助补救；③ 正是本次要消灭的痛点 |
| **D3** | 留空语义 | ① 留空 = 不改动 / ② 留空 = 清空 | **①** | 凭证不可回显，② 会造成不可预期的密钥失效 |
| **D4** | 多 secret 字段 impl | ① JSON 复合 / ② 暂禁选无 Bean 的 impl / ③ 只支持单字段 | **① + 灰字提示**（不禁选） | schema 已预留；禁选会让「先建配置后接实现」不可能 |
| **D4b** | 卡片显示条件由「角色推断」（零后端改动）还是元数据 `credentialKind`（E4）声明 | ① 角色推断 / ② E4 元数据 | **① 先落地，E4 可选精化** | E4 更严谨，但会引入后端 + 前端 + 文档三处改动；角色推断已能覆盖当前 4 个有 Bean 的 impl |
| **D5** | 凭证缺失是否阻断保存 | ① 软提示 / ② 强制校验 | **①**（CALLBACK + 已发布入站接口给硬警告文案） | 保持现有「链失败不污染状态机、配置先于联通」的演进习惯 |

---

## 8. 实施拆解与工作量（方案 A）

| # | 任务 | 产物 | 人日 |
|---|---|---|---|
| 1 | `CredentialEntry.vue`：secret 字段渲染（含复合）、状态行、update/reset 选择、校验与确认 | 新组件 | 0.8 |
| 2 | `Apps.vue` 接线：impl→secretFields 解析、编辑态拉凭证状态、草稿凭证收集、`save()` 两段式与失败补救 | 改造 | 0.8 |
| 3 | 后端 E1（列表凭证角标）+ E2（过期 ROTATING 计数修复） | 小修 | 0.3 |
| 4 | 文案与提示（含 C8/C9 的界面明示、`AdapterParamsEditor` 提示语微调） | — | 0.2 |
| 5 | 文档 6 处同步 + 手动用例 C2/C4 改写 | — | 0.5 |
| 6 | 自测：新建 / 编辑 / 轮换 / 重置 / 无凭证 / 多字段 / 失败补救 7 条路径 | — | 0.4 |
| | **合计** | | **≈ 3 人日** |

---

## 9. 验收要点（评审通过后即作为手动回归清单）

1. **新建带凭证**：新建应用 → 选 `BearerTokenAuthAdapter` 实例 → 填 Token → 保存 → 应用列表出现该行；`GET /apps/{appId}` 的 `credentials[]` 出现 `OUTBOUND / ACTIVE / ****尾4`；库里 `credential` 列为密文（`SELECT` 肉眼确认不可读）。
2. **编辑更新（并存）**：再进弹窗填新值 → 保存 → 旧值 `ROTATING`（`rotating_until ≈ now+24h`）、新值 `ACTIVE`；指纹随之变化。
3. **重置（不可逆）**：切「重置」→ 确认框 → 保存 → 旧行 `RETIRED`、`retired_at` 有值。
4. **回调验签错配防护**：`HmacCallbackVerifyAdapter` 场景，CALLBACK 凭证未配 → 弹窗黄色警告；配好后 `POST /interfaces/{id}/test-callback` 通过。
5. **impl 驱动渲染**：切换适配器（Noop ↔ Bearer ↔ 云厂商签名）→ 凭证卡片出现/消失/变多字段；复合凭证提交后 DB 中为 JSON 明文加密态。
6. **留空不改动**：编辑态不填任何凭证 → 保存后指纹与状态不变。
7. **边界**：接口级 AUTH 覆盖为另一 impl 时，出站请求仍使用应用级 OUTBOUND 凭证（C9 明示）；接口版本回滚后凭证不受影响（C8 明示）。

---

## 10. 附：既有缺陷与文档口径冲突（建议一并修）

| 编号 | 问题 | 证据 | 建议 |
|---|---|---|---|
| D-1 | **过期 ROTATING 永久卡死 `prepare`**：计数未排除 `rotating_until` 已过期的行，`listViews` 显示「已过期」，但用户无法发起新轮换 | `CredentialService.prepare`（`countByStatus(...,'ROTATING')`）、`CredentialRepository.countByStatus` | 落 E2：计数加 `AND (rotating_until IS NULL OR rotating_until > NOW())` |
| D-2 | `prepare` 返回 `CredentialIssuedView(-1, …)`，未回填自增 id，前端无法「生成后立即激活」 | `CredentialService.prepare` 返回 `new CredentialIssuedView(-1, …)`；`CredentialRepository.insert` 未用 `KeyHolder` | 若本期不接 prepare 入口，可延后；接入前必须修 |
| D-3 | **M0-04 §3.2 与 M5 二轮口径冲突**：M0-04 写「凭证随链缓存，变更须触发链缓存失效事件」，M5 已改为「凭证每请求实时读，不进缓存、无需事件」 | `M0-04` §3.2「缓存」条目 vs `ConfigChangedEvent` 注释 / `ChainEngine.injectCredential` | 修订 M0-04 §3.2 该条为「凭证不进链缓存（M5 二轮修订）」，避免后来者按错口径实现 |
| D-4 | M0-04 §3.2 写「ACTIVE 缺失时阻止『删除最后凭证』」；实现侧以「仅 RETIRED 可物理删除 + `retire` 返回告警文案」间接达成 | `CredentialService.delete/retire` | 在 M0-04 中把「删除」明确为「物理删除」，并把 `retire` 的告警返回写入口径 |

---

## 11. 待拍板问题（**已拍板，2026-09-11**）

| # | 问题 | 拍板结果 |
|---|---|---|
| D1 | 凭证归属粒度 | **方案 A**（`(app, kind)` 不变，UI 按 impl 渲染）；B 方案留待真实「同应用多签名密钥」需求 |
| D2 | 新建应用凭证时序 | **前端串行 + 失败补救**（应用已建、凭证失败 → 提示并重新进入编辑补填） |
| D3 | 留空语义 | **留空 = 不改动**；部分填写前端拦截 |
| D4 | 多 secret 字段 impl | **JSON 复合**（键名取元数据 key）+ 未交付实现的 impl 可填但提示 |
| D4b | 卡片显示条件 | **角色推断**（零后端改动）；E4 `ImplMeta.credentialKind` 延后 |
| D5 | 凭证缺失是否阻断 | **不阻断**（软提示）；CALLBACK 且存在已发布入站接口 → 强警告文案 |
| E1 | 列表凭证角标 | **已落地**（`hasOutboundCredential` / `hasCallbackCredential`） |
| E2 | 过期 ROTATING 阻塞 prepare | **已落地**（`countLiveRotating`） |
| E3 | 复合凭证明文另塞 Map | 延后（待云厂商签名适配器实现时一并做） |
| E4 | `ImplMeta.credentialKind` | 延后 |
| — | 是否更新 `API中心原型.html` | 本期不动（原型本用 password 型 secret 字段，语义不冲突） |

---

## 12. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-11 | 初稿（待评审）：现状取证、方案 A/B/C 对比、详细设计、影响面、D1–D5（含 D4b）决策项、既有缺陷 D-1～D-4 |
| v0.2 | 2026-09-11 | 评审通过并落地：拍板结果入 §11、实现记录入 §13、§4.6 时序按实现校正、影响面 6 处文档同步完成 |

---

## 13. 落地记录（2026-09-11）

### 13.1 代码改动

| 层 | 文件 | 改动 |
|---|---|---|
| 前端（新增） | `frontend/src/components/CredentialEntry.vue` | 凭证卡片纯展示组件：状态行（待创建 / 未配置 / ACTIVE / ROTATING·已过期）、字段输入（password + 眼睛）、update/reset 单选、缺凭证提示、复合凭证说明；`fields` 由父组件传入（含通用兜底） |
| 前端（改造） | `frontend/src/views/Apps.vue` | ① 列表新增「凭证」列（出站/回调 已配·缺失，取 E1 布尔）；② 两个适配器下拉下方内联 `CredentialEntry`；③ 新增 `credFields / credentialNeeded / showCredCard / fillCredState / resetCredState / credWarning / buildCredential / collectCredentialDrafts / submitCredentials`；④ `openCreate` 重置凭证态；`openEdit` 改 async，拉详情填凭证态 + 查已发布入站接口；⑤ `save()` 两段式（先应用后凭证）+ reset 二次确认 + 新建失败补救 |
| 前端（文案） | `frontend/src/components/AdapterParamsEditor.vue` | secret 字段灰字提示改指新入口（「新建/编辑应用 → 凭证卡片」） |
| 后端（E1） | `dto/AppDtos.java`、`service/AppService.java`、`repository/CredentialRepository.java` | `AppResponse` 增 `hasOutboundCredential / hasCallbackCredential`；`findActiveKinds(appIds)` 一次 IN 查询批量取（避免 N+1）；`list()` 批量为行填角标、`detail()` 由遮显列表推导 |
| 后端（E2） | `service/CredentialService.java`、`repository/CredentialRepository.java` | 新增 `countLiveRotating`（排除 `rotating_until` 已过期行），`prepare` 改用它——修既有缺陷 D-1 |
| 后端（文案） | `service/AdapterImplCatalog.java` | 5 处 secret 字段 hint 改指新入口 |
| 测试 | `src/test/java/com/deepx/apicenter/M1IntegrationTest.java` | 新增 2 条：`应用凭证角标_列表与详情按ACTIVE存在性标记`（E1）、`过期ROTATING不再阻塞新一轮prepare`（E2） |

### 13.2 验证结果

| 项 | 结果 |
|---|---|
| 后端编译 | `mvn -o compile` 通过 |
| M1 集成测试 | **16/16 通过**（原 14 + 新增 2） |
| 全库测试 | 190 用例中 3 条预存在失败，与本次改动无关（已用 `git stash` 剥离本次改动复现）：`SnapshotChangeDiffTest` 2 条（config_json 解析，属版本变更说明 WIP）+ `M4IntegrationTest.c3` 1 条（对账/补偿回放时序抖动，多次运行失败用例不固定） |
| 前端构建 | `npx vite build` 通过（Apps 分块 21.47 kB / css 1.57 kB） |

### 13.3 实现取舍与偏差（与 §4 设计的差异）

1. **载荷组装在父组件**（`Apps.vue buildCredential`），组件不暴露 `buildPayload`：数据流单向、组件保持纯展示，便于后续复用；
2. **通用兜底字段**：impl 未声明 secret 字段（如 `HmacCallbackVerifyAdapter`）时，`credFields` 返回单字段 `{ key: '__value', label: '密钥 / Token' }`，按单值字符串提交；
3. **状态行取值**：优先 ACTIVE → 未过期 ROTATING → 过期 ROTATING；过期行在卡片上以 `ROTATING·已过期` 提示（与 E2 语义对齐：不再阻塞新轮换）；
4. **列表角标**走 E1 布尔（列表接口仍不带 `credentials` 子表）；**详情抽屉**（原有凭证轮换操作）保持不动，作为高级轮换入口；
5. 未做 E3/E4，未改 `API中心原型.html`（按 §11 拍板）。

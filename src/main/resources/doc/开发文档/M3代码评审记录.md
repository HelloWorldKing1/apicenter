# M3 代码评审记录

> 评审日期：2026-09-03 · 方式：独立评审代理对照《M3开发计划.md》（D-M3-1~4 定稿 / §3 任务 / §4 测试点 / §5 手动方案 / §6 文档同步）逐文件审计已实施代码 + 人工逐条复核取证 · 状态：**2 缺陷 + 4 遗漏 + 1 建议待修复（整改方案已列）；5 条驳回有据**
> 范围：M3 已落地代码（commit `5a2e79c`，9 新增类 + 11 修改处 + 7 测试类 + stub），对照 M3 开发计划核对一致性。
> 基线：全库 103 测试全绿（M1 22 / M2 23 / M3 58）。

## 一、问题清单（按严重度，均待修复）

### 缺陷（建议立即处理）

| # | 位置 | 问题 | 整改方案 |
|---|---|---|---|
| 1 | `engine/ChainEngine` OUTBOUND_AUTH 阶段 | **INBOUND 送达误带应用默认供应商签名头**：assemble 对 INBOUND 不特判，`resolveBound("AUTH")` 回退 `app.auth_adapter_id`（fastmoss = ADP-101 Bearer）+ `injectCredential` 注入出站凭证 → 每次回调送达都向回调地址发 `Authorization: Bearer <供应商 token>`。违背 D-M3-2「OUTBOUND_AUTH 默认 Noop」（设计 §5.3 向回调地址签名默认无），且把面向供应商的密钥外泄给回调地址。未暴露原因：集成测试应用 authAdapterId=null | OUTBOUND_AUTH 阶段 INBOUND 恒走 NoopAuthAdapter（不解析、不注入凭证）；回归：测试应用配置应用级 AUTH=ADP-101，B1 断言送达请求**无** Authorization 头 |
| 2 | `engine/InboundEngine.deliver` | **送达环节 DB 异常破坏「ack 恒 200」**：`updateState`/`insertDeadLetter` 不在 try 内，DB 抖动经 deliver → handle → gateway 冒泡成 500，违背 ack 与送达解耦定稿 | deliver 内运行表写入整体 try/catch（log.error 记异常、吞掉不冒泡），保证无论送达侧任何异常都回 ack |

### 遗漏（计划点名未做）

| # | 位置 | 问题 | 整改方案 |
|---|---|---|---|
| 3 | 测试缺失 | **>1MB → 40002 双向限制零测试**（§4 安全测试点；实现有 `GatewayController.max-body-bytes`，无验证） | M3IntegrationTest 补：出站路径 >1MB → 信封 40002；入站回调路径 >1MB → 40002 |
| 4 | `controller/admin/InterfaceController.test` | **后端 `/test` 端点无 OUTBOUND 门控**（前端已隐藏按钮，直连后端仍可把 INBOUND 接口当出站执行——upstreamPath=null 拼出非法 URL） | test() 加 `ifType != OUTBOUND → 40001` 守卫 |
| 5 | 测试缺失 | **extractEventId 的 Header 兜底与 NULL 分支无单测**（仅覆盖报文 event_id 命中） | InboundEngineTest 补三用例：报文 event_id 优先 / 无字段时 Header X-Event-Id 兜底 / 皆无 → NULL |
| 6 | `engine/InboundEngine.redeliver` | **无「仅 PENDING 才认领」守卫**：并发双扫（调度 + 手动 scan）可在记录已 ACKED/DEAD 后重复送达 | `incrementAttempt` 改为条件认领 `UPDATE ... SET attempt_count=attempt_count+1 WHERE id=? AND delivery_status='PENDING'`，0 行受影响即放弃重放 |

### 建议（低成本加固）

| # | 位置 | 问题 | 整改方案 |
|---|---|---|---|
| 7 | `adapter/protocol/XmlProtocolAdapter.readElement` | 递归无深度上限，万层嵌套（≤1MB 内可构造）可栈溢出（被外层 catch 成 40002，属健壮性而非漏洞） | 递归带深度参数，>512 层抛 40002 |

## 二、驳回项（有据，不改）

| 代理提出 | 驳回理由 |
|---|---|
| 适配器参数注入时序问题 | 自撤回——`AdapterInstance.process` 在每阶段执行前注入 adapterParams，INBOUND_AUTH 阶段可正常读到验签参数 |
| 空 body 解码语义不一致 | 自撤回——空 body → 空对象与 JSON 适配器行为一致 |
| 「开发计划.md 未更新 M3 状态/回链」 | **不实**——`5a2e79c` 提交已含开发计划.md M3 状态更新（回链在 `7e80652` 已加），代理漏查 |
| SSRF 重定向绕过 | **不成立**——JDK HttpClient 默认不跟随重定向（Redirect.NEVER），无重定向攻击面 |
| RESP 未知声明类型不告警 | 宽松策略已定稿（D-M3-3 仅转换失败/结构不符记 warning），不改 |

## 三、达成核对（计划 → 代码一致）

- **D-M3-1 XML 语义**：同名合并（单次保单值）/ 空元素↔NULL 往返 / 属性→attributes（JSON 输出丢弃）/ CDATA / 混合内容 40002 / DTD·实体拒绝（探针 + 工厂属性双保险）/ 命名空间剥离 / 根元素约定（request/response 按方向）/ paramTypes 仅顶层 —— 全部落地并有单测矩阵；
- **D-M3-2 入站引擎**：PENDING 首落（next_retry_at=now+5s 防抢跑）/ attempt_count 口径（首送 1 + 重放 +1，短重试不计）/ max_attempts=max_retries+1 / ack 解耦 / 4xx 直接死信（reason 文案）/ 补偿按快照重放（Content-Type 按接口当前 protocol_out 推导）/ SSRF 保存+运行时双校验+开关 / callback_event_id 提取顺序 / trace_id 无则生成 / 生命周期校验（40102/40401）/ 验签（缺失 40100、过期 40101、轮换并存、防重放窗口、常量时间比较）—— 除缺陷 1/2 外全部一致；
- **D-M3-3**：RESP 矩阵（string/number 兼容与 STRING 解析/boolean/date/array·object 结构不符保原值+warning）/ 仅成功路径 / data 非对象不过滤 / 空声明不过滤；ACK 字段名可配值固定 / sort_order 排序 / >2 忽略+warning / type 决定写法 / 无声明兜底 / 按 protocol_in 渲染（XML 根元素 response）—— 全部一致；
- **D-M3-4 响应收敛**：OutboundEngine 内联解析已彻底移除、decodeResponse 宽松（2xx 解析失败按空对象）、错误响应不解码、G1-G4 行为等价回归全绿 —— 一致；
- **§4 测试点**：B1-B6 / X1-X3 / 单测矩阵全覆盖（>1MB、extractEventId 分支除外，见遗漏 3/5）；
- **§5 手动方案与 stub**：WireMock stub 随仓库（xml-supplier 含 ts 过滤演示 / delivery-ok / fail / flaky 前 8 次 500）；手动步骤与代码语义一致（M3DEMO 应用名、发布步骤、curl 样例）；
- **§6 文档同步**：CLAUDE.md 状态表与测试计数 / 技术架构 §4.1.1 技术债收敛表述 / 开发计划.md M3 状态与回链 / M3开发计划.md 状态 —— 全部完成。

## 四、顺带闭环与遗留

- **M2 评审 N4 闭环** ✅：接口删除守卫补查 inbound_delivery 已由 M3 落地（`InterfaceService.delete`）；
- **M2 评审 #13 部分闭环**：XmlProtocolAdapter、HmacCallbackVerifyAdapter 已注册实现（出站 HMAC / 云厂商签名 / 云厂商回调验签仍是幽灵条目，属多鉴权并行线）；
- **遗留**：M3 手动验收方案（§5，约 25 分钟）尚未在平台实际执行（自动化双通道之一待跑）；缺陷 1/2 与遗漏 3-6、建议 7 待修复后回归。

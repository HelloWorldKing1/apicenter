{
  "name": "apicenter-admin",
  "version": "0.1.0",
  "private": true,
  "description": "API 中心管理面(Vue3 + Element Plus,原型交互平移)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:unit && npm run test:ssr",
    "test:unit": "node --test \"src/utils/**/*.test.mjs\"",
    "test:ssr": "vite build --ssr test/ssr-smoke.mjs --outDir dist/ssr-smoke --emptyOutDir --logLevel error && node dist/ssr-smoke/ssr-smoke.mjs"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "echarts": "^5.6.0",
    "element-plus": "^2.8.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.0",
    "vite": "^5.4.0"
  }
}

# API 中心 · 开发计划

> 依据《技术架构和实现方案.md》M1–M5 路线图、《可行性报告.md》81 人日估算与风险表编制。
> 执行原则：**M0 先定稿三个重点设计项（动态映射边界 / 对账协议 / 通用客户端约定），再进 M2**；M2 为难度峰值（链引擎 + 映射引擎），单独出契约设计后再动手。

## 1. 里程碑总览

| 里程碑 | 内容 | 估时 | 依赖 | 难度 | 出口标准 |
|---|---|---|---|---|---|
| M0 设计定稿 | 三个重点设计项定稿 + 链引擎 / 映射引擎契约设计 | 3 | — | 中 | 三份契约设计通过评审 |
| M1 骨架 | 管理面框架 + app/group/interface CRUD + 生命周期 + 凭证管理 + 建表 | 8 | M0 | 低 | 应用 / 分组 / 接口配置闭环 |
| M2 出站链路 | 链引擎 + JSON 编解码 + 映射引擎 + 状态机 / 重试 / 补偿 / 死信 | 15 | M1 | **中-高（峰值）** | json-json 出站链路端到端跑通（含 4 种核心鉴权） |
| M3 协议与入站 | XML 编解码 + 入站回调链路 + 四种转换场景补全 | 12 | M2 | 中 | json-xml 与入站回调端到端 |
| M4 容错与监控 | 熔断 / 对账 / 死信重放 / 日志·指标·链路·告警 | 10 | M2、M3 | 低-中 | 可靠性底座与可观测齐备 |
| M5 版本与灰度 | 版本快照 / 回滚 / 灰度 / 压测调优 | 8 | M4 | 中 | 支持回滚与灰度发布 |
| 多鉴权（并行） | 10 种鉴权分三批，与 M1–M3 并行 | 18 | M0 起 | 中 | 10 种鉴权全部可用 |
| 联调验收 | mock 对端先行 → 真实供应商联调 → 验收 | 10 | M4、M5 | 中 | 验收通过 |
| **合计** | | **84** | | | 单人约 4 个月；2 人并行（主线程 M0–M5 56 + 副线程鉴权 / 联调 28）约 2.5 个月 |

> 说明：可行性报告原估 81 人日未含 M0 设计定稿，本计划补入 M0（3 人日）后合计 84 人日。

## 2. 里程碑明细

### M0 设计定稿（3 人日）

| 任务 | 人日 |
|---|---|
| 链引擎契约设计：Adapter 接口（type/order/supports/process）、AdapterContext、链编排、绑定继承 / 覆盖解析、协议自动推导规则 | 1 |
| 动态映射规则语义规范：6 操作 × param 语法、类型注册表边界行为（number→DATE 等定死）、condition 表达式沙箱约束 | 1 |
| 通用声明式客户端约定（按协议维度接口 + 动态 URI / 凭证组装）+ UNKNOWN 对账查询协议与兜底策略 | 1 |

**测试点**：契约设计评审通过（评审记录）；映射语义规范覆盖 6 操作 × 4 空值策略全部组合的预期输出定义。

### M1 骨架（8 人日）

| 任务 | 人日 |
|---|---|
| 管理面框架（原型交互平移）+ 导航 / 弹窗 / 表单组件 | 2 |
| app / app_group / interface CRUD + 生命周期状态机 + 两级下拉 + 类型互斥校验 | 3 |
| 凭证管理（加密存储 / 遮显回显 / 轮换）+ 适配器注册表骨架 | 2 |
| schema.sql 落库 + 初始化数据（对应原型 4 应用 / 4 接口） | 1 |

**测试点**：
- 单测：应用生命周期流转（草稿→启用→停用→注销，停用即拒请求）；接口类型互斥（OUTBOUND 禁 callback_url / ack 字段，INBOUND 必填回调地址与出站侧送达报文）；凭证加密落库 + 界面遮显；分组归属校验（组必须属于所选应用）
- 集成：15 张表 DDL 可重复执行；配置 CRUD 全链路

### M2 出站链路（15 人日）—— 难度峰值

| 任务 | 人日 |
|---|---|
| 适配器链引擎（契约 + Context + 链组装 + 绑定解析 / 继承覆盖 + 灰度索引） | 5 |
| 动态字段映射引擎（6 操作 + param + 表达式沙箱 + 类型注册表） | 5 |
| 通用声明式客户端（动态 URI / 凭证组装 + 连接池）+ JSON 编解码（每应用独立实例） | 2 |
| 出站状态机 + @Retryable Invoker（熔断闸门前置）+ 补偿 worker + 死信 | 3 |

**测试点**：
- 单测：映射 **6 操作 × 4 空值策略矩阵（≥24 例）**；状态机每条边（成功 / 4xx 死信 / 5xx 重试 / 重试耗尽补偿 / 超时 UNKNOWN）；绑定解析（继承 / 覆盖 / 协议自动推导）；**表达式注入用例（沙箱拦截）**；类型转换边界
- 集成：outbox 先记后发；补偿 worker 按 (status, next_retry_at) 扫描重试；死信落库 + 重放；QPS 限流 / 配额拒绝不污染状态机

### M3 协议与入站（12 人日）

| 任务 | 人日 |
|---|---|
| XML 编解码（根元素 / 命名空间 / 属性映射 / 空值策略） | 2 |
| 入站回调链路：回调验签 → 映射 → 送达（回调地址快照）→ ack 回执 → 补偿重送 | 6 |
| 出站响应字段 / ack 回执字段解析 | 2 |
| 四种转换场景补全（json-xml / xml-xml / xml-json） | 2 |

**测试点**：
- 单测：XML 边界（空值 / 命名空间 / 属性）；回调验签失败 401 且不落送达记录；**ack 回执与送达解耦（送达失败仍回 ack，供应商不重发）**；重送按 callback_url_snapshot 不随接口改址漂移
- 集成：补偿 worker 重送 PENDING→ACKED / 重送耗尽→DEAD_LETTER；四种转换场景端到端各 ≥1 例

### M4 容错与监控（10 人日）

| 任务 | 人日 |
|---|---|
| 熔断器（三态 + 闸门置于 @Retryable Invoker 调用前） | 3 |
| UNKNOWN 对账（上游查询协议 + 收敛策略 + 无查询能力人工兜底） | 3 |
| AOP 日志 + Micrometer/Prometheus + OTel 节点 span + 告警规则 | 4 |

**测试点**：
- 单测：熔断三态流转（CLOSED→OPEN→HALF_OPEN）；**OPEN 时短路、不触发 @Retryable**；对账收敛三分支（已成功→SUCCESS / 未到达→COMPENSATING / 无法查询→人工兜底）
- 集成：日志脱敏（手机号 / 密钥 / Header 敏感值）；traceId 贯穿出站与入站且与运行表 trace_id 关联；告警触发（成功率下降 / 死信堆积 / 补偿失败）

### M5 版本与灰度（8 人日）

| 任务 | 人日 |
|---|---|
| 接口版本快照 + 回滚 | 3 |
| 适配器 impl+version 灰度绑定 + 灰度路由全程留痕 | 3 |
| 压测与调优（连接池 / 补偿批量扫描 / 日志异步写） | 2 |

**测试点**：快照回滚一致性（回滚后行为与快照一致）；灰度按比例路由且留痕；压测：QPS 限流阈值生效、补偿批量扫描性能、日志异步写不阻塞主链路。

### 多鉴权（并行 18 人日）

| 批次 | 范围 | 人日 |
|---|---|---|
| 一批（首期） | HMAC（复用既有 SignatureService）/ API Key / 云厂商签名（TC3/SigV4/ACS3）/ 云厂商回调验签 | 8 |
| 二批 | OAuth2 Client Credentials / Bearer / Basic | 6 |
| 三批 | OAuth2 授权码 / mTLS | 4 |

**测试点**：每规范签验签用例 + 时间戳容差 / 防重放；**云厂商签名用官方示例向量测试**（TC3/SigV4/ACS3 各至少一组官方向量）；连续验签失败告警 / 封禁。

### 联调验收（10 人日）

| 任务 | 人日 |
|---|---|
| mock 对端先行联调（本地 mock 服务模拟供应商与回调地址） | 4 |
| 真实供应商联调（腾讯云 / 阿里云出站 + 回调验签） | 4 |
| 验收用例执行 + 文档 | 2 |

**测试点**：功能清单 → 验收用例逐条映射（F1–F7）；边界用例（空值 / 超长 / 特殊字符 / 并发回调）。

## 3. 测试策略总览

| 层级 | 范围 | 载体 |
|---|---|---|
| 单测 | 映射矩阵（6×4）、状态机边、验签、熔断三态、类型互斥校验、表达式沙箱 | JUnit |
| 集成 | outbox / 补偿扫描 / 死信重放 / 对账收敛 / 绑定解析 | @SpringBootTest + mock 对端 |
| 联调 | 真实供应商出站 + 回调验签 | mock 先行，再换真实 |
| 压测 | QPS 限流 / 补偿批量 / 日志异步（M5） | 压测工具 |
| 安全专项 | 凭证加密与脱敏、表达式注入、回调地址 SSRF、防重放 | 专项用例清单 |

## 4. 里程碑节点与交付物

| 节点 | 交付物 |
|---|---|
| M0 完成 | 三份契约设计（链引擎 / 映射语义 / 客户端约定 + 对账协议） |
| M1 完成 | 配置闭环可演示版本 |
| M2 完成 | **首个可用版本：json-json 出站链路端到端**（含 4 种核心鉴权） |
| M3 完成 | 双协议 + 入站回调闭环 |
| M4 完成 | 可靠性底座 + 可观测齐备 |
| M5 完成 | 灰度 / 回滚 + 压测报告 |

## 5. 关键依赖与风险提示

- **前置（开工前）**：三个重点设计项定稿（对应技术方案 §7 结论第 4 条）。
- **峰值对冲**：M2 的链引擎 + 映射引擎约占 10 人日，是唯一需要「先设计后编码」的部分——M0 契约评审不通过不得进入 M2。
- **并行约束**：多鉴权从 M0 后即可并行，但云厂商签名 / 回调验签需真实密钥，联调排在 M3 后。
- **新增安全测试点**：condition 表达式注入（动态映射引擎引入的新攻击面，demo 无表达式引擎故此前未暴露），已在 M2 单测与安全专项中列明。
